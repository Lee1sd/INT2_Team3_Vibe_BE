llm:
  mode: mock
  model: claude-haiku-4-5
  mock:
    score-per-question: 18

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/career_dungeon?useUnicode=true&characterEncoding=UTF-8&serverTimezone=Asia/Seoul&allowPublicKeyRetrieval=true
    username: root
    password: ''   # ← 본인 MySQL root 비밀번호 입력 후 application-local.yml 로 저장
    hikari:
      connection-timeout: 30000
      maximum-pool-size: 10
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  security:
    oauth2:
      client:
        registration:
          google:
            client-id: 'your-google-client-id'        # ← Google Cloud Console OAuth2 클라이언트 ID
            client-secret: 'your-google-client-secret' # ← Google Cloud Console OAuth2 클라이언트 시크릿
            redirect-uri: '{baseUrl}/api/auth/oauth2/callback'
            scope:
              - email
              - profile

jwt:
  secret: 'Y2FyZWVyZHVuZ2Vvbi1qd3Qtc2VjcmV0LWtleS1mb3ItbG9jYWwtZGV2ZWxvcG1lbnQ='
  # ↑ 로컬 전용 테스트 시크릿 (Base64 인코딩, 최소 32바이트).
  # 운영 환경에서는 반드시 새 랜덤값으로 교체하세요.

cors:
  allowed-origins: 'http://localhost:3000'  # 프론트 개발 서버 포트에 맞게 조정

auth:
  cookie:
    # 로컬은 FE(3000)/BE(8080) 둘 다 HTTP localhost라 Secure 쿠키가 저장되지 않는다.
    # Secure=false + SameSite=Lax여야 로그인 후 새로고침/POST /api/auth/refresh가 동작한다.
    # 배포(HTTPS, cross-site)는 application-prod.yml에서 Secure=true + SameSite=None을 쓴다.
    secure: false
    same-site: Lax

aws:
  s3:
    bucket: 'your-dev-bucket-name'  # ← 본인 개발용 S3 버킷 이름으로 변경 (ADR-018, 마이페이지 프로필 이미지)
  # 자격증명은 이 파일에 두지 않는다 — AWS SDK 기본 자격증명 체인(환경변수 AWS_ACCESS_KEY_ID/
  # AWS_SECRET_ACCESS_KEY 또는 ~/.aws/credentials)을 그대로 쓴다. .env.example 참고.
