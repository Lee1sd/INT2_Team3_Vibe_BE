# Career Dungeon 백엔드의 공통 애플리케이션 설정을 관리한다.
spring:
  application:
    name: career-dungeon
  profiles:
    default: local
  jpa:
    open-in-view: false
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB

auth:
  cookie:
    # refreshToken 쿠키 Secure/SameSite 기본값 — local·test 프로필 기준. FE(3000)/BE(8080)가
    # 둘 다 HTTP localhost라 Secure 쿠키는 저장되지 않으므로 SameSite=Lax + Secure=false를
    # 쓴다. prod는 application-prod.yml에서 Secure=true + SameSite=None으로 덮어쓴다(이슈 #117).
    secure: false
    same-site: Lax

aws:
  # 리전은 환경 공통값 — 버킷명은 환경별로 다르므로 profile별 yml에서 채운다(ADR-020).
  region: ap-northeast-2

llm:
  mode: mock
  model: claude-haiku-4-5
  anthropic:
    base-url: https://api.anthropic.com
    version: 2023-06-01
    max-tokens: 2048
    connect-timeout: 5s
    read-timeout: 30s
