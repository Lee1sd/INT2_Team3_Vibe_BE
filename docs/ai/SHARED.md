# SHARED.md — Career Dungeon 팀 공통 AI 작업 규칙

> 이 문서는 `.claude/hooks/session-context.py`가 `SessionStart` 훅으로 매 세션 시작 시
> 자동으로 컨텍스트에 주입합니다. 직접 `@` 하지 않아도 됩니다. 다만 훅이 실패하거나
> 오래된 세션을 이어가는 경우를 대비해, 이 문서는 사람이 읽어도 완결된 내용이어야 합니다.

## 0. 프로젝트 한눈에 보기

| 항목 | 내용 |
| --- | --- |
| 인원 | 4명, 전원 백엔드 겸임. 표지민은 인증+인프라+프론트+공통코드까지 겸임 |
| 기간 | 3주 실개발 (기획 7/8 동결 → 이후 요구사항 변경 없음) |
| 아키텍처 | 단일 모놀리스, 도메인 중심 패키지 분리 (`domain.*`, `global.*`) |
| 최대 리스크 | LLM 응답 불안정성(스키마 이탈, 점수 범위 이탈), 3주 일정 준수 |
| 외부 의존 | LLM API(Claude), AWS S3, Apache PDFBox, Google OAuth2 |
| 기본 방어선 | **Mock LLM 모드가 기본값.** 실 API 호출은 통합테스트/데모에 한정 |

이 표는 왜 이 하네스가 FANDROPS와 다르게 생겼는지의 근거입니다. FANDROPS(5명 도메인 전문화,
7주, Gradle 멀티모듈, 오버셀/동시성 방어 중심)에서 검증된 **패턴**(계층 분리, 경로 라우팅,
역방향 추적, Retro 루프)은 그대로 채용했지만, **내용**은 이 표에 맞게 전부 새로 썼습니다.

## 1. 대화 시작 시 확인 순서

1. 이 문서(SHARED.md)는 이미 주입되어 있습니다. 다시 요청하지 마세요.
2. 본인 owner 파일을 `@docs/ai/owners/<본인 이름>.md`로 멘션하세요. (자동 주입되지 않습니다)
3. 작업 대상 코드 경로가 `docs/ai/README.md`의 "경로 → 오너" 표와 일치하는지 확인하세요.
   본인 담당이 아닌 경로를 수정해야 한다면, 먼저 그 owner에게 알리거나 팀 채널에 공지하세요.

## 2. 6단계 사고 절차

모든 기능 구현·버그 수정 작업은 아래 순서를 따릅니다. 순서를 건너뛰지 마세요.

1. **Brainstorm** — 요구사항명세서(FR/NFR)와 API 명세서에서 관련 항목을 찾고, 가능한 접근을
   최소 2가지 이상 떠올립니다. LLM을 호출하는 기능이라면 "Mock 모드에서 어떻게 동작할지"를
   먼저 정의합니다.
2. **Plan** — 변경할 파일 목록, 엔티티/DDL 영향, API 응답 계약 변경 여부를 문장으로 먼저
   적습니다. 표지민이 확정한 공통 응답 포맷/예외 계약을 벗어나지 않는지 확인합니다.
3. **Execute** — 계획한 범위만 구현합니다. 담당 owner 파일의 "손대지 말 것" 경로는
   건너뛰고, 필요하면 해당 owner에게 요청합니다.
4. **역방향 추적 자가검증 (6문항)** — 아래 §3의 6개 질문에 전부 답할 수 있어야 커밋합니다.
5. **Debug** — 실패하면 재현 조건을 먼저 문장으로 적고, 관련 로그/스택트레이스를 확인한
   뒤 수정합니다. 추측성 수정을 반복하지 않습니다.
6. **Retro** — 작업이 끝나면 §5 형식의 `[Retro]` 한 줄을 출력합니다.

## 3. 역방향 추적 체크리스트 (6문항)

FANDROPS의 4문항(Spring Boot 공통)을 유지하고, Career Dungeon 특화 리스크 2개(⑤⑥)를 추가한
버전입니다. 이 프로젝트가 실제로 겪는 결함을 보고 항목을 추가/수정하면 그 자체가 §6 Retro
루프의 결과물입니다.

| # | 질문 | 확인 방법 | 근거 |
| --- | --- | --- | --- |
| ① DB 제약 | 이 변경이 요구하는 제약(UNIQUE, FK, NOT NULL 등)이 실제 DDL/엔티티에 있는가? | 엔티티 클래스와 `docs/erd/entity-definition.md` 대조 | 엔티티정의서 |
| ② 예외 핸들러 | 새로 던지는 예외가 어떤 `@ExceptionHandler`/`@RestControllerAdvice`에서 잡히는가? | `global.exception` 패키지 grep | Spring Boot 공통 |
| ③ 직렬화 설정 | `Instant`/점수/열거형 필드의 직렬화 결과가 API 명세서의 응답 예시와 정확히 일치하는가? | `application.yml`, 실제 JSON 응답 확인 | Spring Boot 공통 |
| ④ 테스트 파일 | 새 Service/Client/Controller에 대응하는 테스트가 있는가? | `src/test/java/...` 확인 | Spring Boot 공통 |
| ⑤ LLM 응답 방어 | 이 코드가 호출/소비하는 LLM 응답이 스키마를 벗어나면(필드 누락, 점수 범위 초과) 어떻게 되는가? Mock 모드에서도 동일 분기가 동작하는가? | `LlmClient` 인터페이스의 예외/재시도 경로 추적 | NFR-05, NFR-12 |
| ⑥ 비용/시간 가드 | 이 코드가 LLM을 호출한다면 예산 상한·타임아웃·재시도 횟수 제약을 지키는가? | `docs/operations/llm-cost-policy.md` 대비 확인 | NFR-11 |

담당 도메인이 LLM을 직접 호출하지 않는다면(예: `resume`, `auth`) ⑤⑥은 "해당 없음"으로 명시하고
넘어가도 됩니다. 단, 명시적으로 "해당 없음"이라고 말해야 합니다 — 조용히 건너뛰지 않습니다.

## 4. Git / 협업 컨벤션 (팀 확정, 아래 4가지가 SSOT)

1. **브랜치 전략**: `main` / `feat/{이슈번호}-{기능명}` / `fix/{이슈번호}-{내용}`
   (예: `feat/23-resume-upload`, `fix/31-parse-status-null`). `main`에 직접 커밋하지 않습니다.
   **예외**: 순수 문서 정리(코드 변경 없음)는 이슈 없이 가벼운 브랜치명(`docs/{내용}`)으로
   진행할 수 있습니다. 코드 변경이 하나라도 포함되면 이 예외를 적용하지 않고 이슈+
   `feat`/`fix` 브랜치명 규칙을 그대로 따릅니다.
2. **커밋 언어와 단위**: 모든 커밋 메시지는 한국어로 작성합니다. `git commit` 실행 직전,
   메시지가 영어로 되어 있지 않은지 스스로 확인합니다. 이 규칙은 몇 개 커밋마다 한 번이 아니라
   매 커밋마다 적용합니다. 여러 작업 단계를 하나의 커밋으로 뭉치지 않고, 기능 구현, 리뷰 반영,
   문서 정리처럼 구현 단계마다 별도 커밋으로 나눕니다.
3. **커밋 컨벤션**: 커밋 메시지 접두사는 아래 6종만 사용합니다. 형식은
   `<type>: <한 줄 요약>` (예: `feat: 이력서 업로드 API 추가`).
   | type | 용도 |
   | --- | --- |
   | `feat` | 새 기능 |
   | `fix` | 버그 수정 |
   | `refactor` | 동작 변경 없는 구조 개선 |
   | `docs` | 문서 변경 (이 저장소의 `docs/`, `CLAUDE.md` 등) |
   | `test` | 테스트 추가/수정 |
   | `chore` | 빌드/설정/의존성 등 그 외 |

   접두사 뒤에는 "무엇을"보다 "왜"를 남깁니다. "무엇을"은 diff로 충분한 경우 생략합니다.
   `git commit`을 실행하기 직전, 아래를 매 커밋마다 스스로 확인합니다.

   1. 메시지가 `<type>: <한글 한줄요약>` 형식인가?
      (type은 `feat`/`fix`/`refactor`/`docs`/`test`/`chore` 중 하나)
   2. 메시지가 한국어로 작성됐는가?
   3. 이 커밋이 여러 작업을 뭉친 게 아니라 하나의 논리적 단위인가?

   하나라도 아니면, 커밋하기 전에 먼저 고칩니다. "일단 커밋하고 나중에 정리"하지 않습니다.
4. **PR 규칙**: PR 제목과 본문은 한국어로 작성합니다. **최소 1인 리뷰 승인 후 머지**합니다. 승인 없이 머지하지 않습니다
   (주말 자가 머지 예외 조건은 `docs/ai/workflows/code-review-culture.md` §5 참고).
   (기획서 12장 "도메인 코드 고립" 리스크 대응 — PR 머지 전 1인 승인으로 크로스 리뷰를
   강제해 도메인 간 경계 침범/누락을 조기에 잡습니다). PR 제목은 연결된 이슈 제목과 동일하게
   작성합니다. 이슈에 `[FEAT]`, `[FIX]` 같은 접두사가 있으면 그대로 유지하고, 임의로 요약하거나
   다르게 짓지 않습니다. PR 본문은 `.github/PULL_REQUEST_TEMPLATE.md`를 먼저 읽고, 그 파일의
   마크다운 구조(체크박스 `[ ]` 포함)를 문자 그대로 복사해서 시작합니다. 섹션 제목이나 체크박스
   문법을 임의로 바꾸지 않습니다. 모든 체크박스 항목은 `[x]` 또는 `[ ]`로 표시하며, 해당 없으면
   `[x]`로 체크하고 `(해당 없음)` 사유를 덧붙입니다. `API 변경 사항` 섹션이 있는 템플릿에서 실제로
   API가 추가/변경된 PR이면 요청/응답/에러 예시를 구체적으로 채우고, `본문 참고`처럼 다른 섹션을
   가리키는 것으로 대체하지 않습니다. PR 생성 직후 완성된 본문을 원본 템플릿과 섹션 단위로 대조해
   빠진 섹션/내용이 없는지 재확인하고 보고합니다. AI가 초안을 만들 때는
   `docs/ai/workflows/templates/pr-body.md`를 참고하되, 최종 본문은 `.github/PULL_REQUEST_TEMPLATE.md`
   구조와 일치해야 하며 `gh pr create --body-file`로 생성합니다.
   PR을 올리기 전 셀프 리뷰, CodeRabbit(AI)과 사람 리뷰의 역할 분담, 코멘트 이모지
   룰은 `docs/ai/workflows/code-review-culture.md`가 SSOT입니다.
5. **이슈 트래킹**: 이슈 제목과 본문은 한국어로 작성합니다. 작업 단위는 GitHub Issues로 관리합니다. 이슈를 만들 때 **담당자
   (assignee)를 반드시 지정**합니다 — 담당자 없는 이슈로 작업을 시작하지 않습니다.
   템플릿은 `docs/ai/workflows/templates/issue-feature-body.md`(기능) 또는
   `docs/ai/workflows/templates/issue-bug-body.md`(버그)를 정확히 따릅니다. 기능 이슈는 섹션 제목
   `# Feature`, `## 기능 설명`, `## 배경 및 목적`, `## 요구 사항`, `## API 설계`,
   `## 데이터 및 도메인`, `## 보안 및 검증`, `## 완료 조건`, `## ADR 필요 여부`,
   `## 참고 자료`를 그대로 사용합니다. 단, 순수 문서 정리처럼 가벼운 작업은 무거운 섹션(API 설계 등)을
   생략해도 됩니다. 브랜치명의 `{이슈번호}`는 이 이슈 번호와 반드시 일치해야 합니다.
6. **PR/이슈 본문 전달 방식**: 한글이 포함된 PR/이슈 본문은 생성(create)이든 수정(edit)이든
   파이프(`-`)로 직접 넘기지 말고, 반드시 UTF-8 임시 마크다운 파일로 먼저 작성한 뒤 `--body-file`
   옵션으로 전달합니다. `gh issue create`, `gh issue edit`, `gh pr create`, `gh pr edit` 전부
   동일하게 적용합니다.
7. **코드 주석 언어**: 코드 주석은 한국어로 작성합니다. 변수명/함수명 등 코드 자체는 영어 컨벤션을
   유지하고, 설명 주석만 한국어로 작성합니다.

공통 응답 포맷/예외 계약은 표지민이 1주차 중 확정합니다. 확정 전까지는 임시 계약임을
PR에 명시하고, 확정 후에는 반드시 맞춰서 수정합니다 (요구사항명세서 CM-002 참고).

## 4-1. AI 실험/트러블슈팅 기록

- 프롬프트/모델/채점 등 서비스 Claude API 활용 관련 시행착오는
  `docs/ai/ai-experiment-log.md`에 기록합니다.
- Claude Code/Codex 하네스 사용 중 겪은 문제(SSOT 검증, 리뷰 반영 오염 방지, 도메인 경계 문제 등)는
  `docs/ai/harness-troubleshooting.md`에 기록합니다.
- 회고를 기다리지 않고 사건 발생 직후 기록합니다.
- 단순 오타/변수명 수정은 기록하지 않습니다.

## 4-1-1. 실 Claude API 호출 기록 (필수, 매번)

- 실제 Claude API(Haiku/Sonnet 등)를 호출한 직후, `docs/ai/api-usage-log.md`에 즉시 한 줄
  추가한다. "나중에 몰아서 기록"하지 않는다.
- 기록 항목: 사용 날짜, 조회 날짜, 사용자, 사용모델, 사용횟수, 내용
- `ai-experiment-log.md`에 실험 배경/발견을 남길 때도, `api-usage-log.md` 기록을 빠뜨리지
  않았는지 함께 확인한다.

## 4-2. PR 생성 전 필수 확인

1. `git fetch origin`으로 main 최신 상태를 확인한다.
2. 현재 브랜치가 main보다 얼마나 뒤처졌는지 확인한다(`git log main..HEAD`, `git log HEAD..main`).
3. 특히 자주 충돌나는 파일(`ai-experiment-log.md`, `api-usage-log.md`)이 main에서 바뀌었으면
   미리 병합한다.
4. 병합 후 번호(`2-N` 등) 중복이 없는지 확인한다.
5. 테스트 통과를 확인한 뒤 PR을 생성한다.

## 5. Retro 형식 (경량화 버전)

FANDROPS는 PR마다 `generated/retro-<PR번호>.md`를 쌓았지만, 3주짜리 프로젝트에는
과합니다. Career Dungeon은 다음 3단계로 경량화합니다.

1. **세션 종료 시**: 아래 형식으로 대화창에 한 줄만 출력합니다.

   ```
   [Retro] 룰 작동: <이번에 하네스 규칙이 실제로 도움이 됐는지>
          | 튜닝 제안: <SHARED.md/owner 파일에 추가·수정하면 좋을 항목>
          | SSOT 동기화 필요: <설계 문서와 실제 코드가 어긋난 부분이 있었는지>
          | Edge Case: <이번 작업에서 새로 발견한 예외 상황>
   ```

   `feat/*`/`fix/*` 브랜치에서는 이 출력을 `.claude/hooks/retro-reminder.py`가
   감지해서 **자동으로** `docs/ai/workflows/generated/retro-raw.md`(개인 로컬 파일,
   커밋 안 함)에 날짜·브랜치와 함께 append합니다. 따로 파일에 저장할 필요는
   없지만, 반드시 이 형식 그대로 출력해야 훅이 인식합니다 — 형식을 바꾸면 자동
   수집이 안 되고 사람이 채팅 기록에서 직접 찾아야 합니다.
2. **매주 금요일**: 4명 각자의 `retro-raw.md`를 공유하고, 한 명이
   `docs/ai/workflows/templates/retro-week-body.md` 템플릿에 맞춰 **카테고리별로
   분류·중복 제거해서** `docs/ai/workflows/generated/retro-week{N}.md` 1개 파일로
   압축합니다 (시간순으로 그냥 이어붙이지 않습니다 — 자세한 절차는
   `docs/ai/workflows/generated/README.md` 참고, 4주 프로젝트라면 최대 파일 4개).
3. **반영**: 압축본에서 반영하기로 한 개선사항은 다음 주 시작 전
   `SHARED.md`/`owners/*.md`에 바로 반영하고, 반영 근거를 ADR 1개로 남깁니다
   (`docs/adr/ADR-002-ai-agent-harness-engineering.md`의 "결정 이력" 절에 추가).

## 6. 절대 규칙

- LLM을 직접 호출하는 새 코드를 작성하기 전에는 반드시 Mock 구현체로 먼저 동작을 확인합니다.
- `LlmClient` 인터페이스를 거치지 않고 특정 벤더 SDK를 도메인 코드에서 직접 참조하지 않습니다.
- 점수·게이지 값은 서버에서 항상 clamp합니다. LLM이 범위 밖 값을 반환해도 그대로 저장하지 않습니다.
- `docs/ai/owners/*.md`는 코드 오너 규칙(담당 경로, 금지 경로, 참고 문서 목록)만 담습니다.
  진행 상황(완료/진행중/미착수), 체크리스트, 정책 수치, 그 외 "지금 상태"를 나타내는 내용은 넣지 않습니다.
  진행 상황은 별도 진행 문서에, 정책 수치는 해당 운영 문서에 둡니다.
- `docs/ai/owners/`를 "AI 면접관 페르소나" 설정으로 착각하지 않습니다. 그건 `domain.persona`입니다.
- 담당이 아닌 owner의 "손대지 말 것" 경로를 임의로 수정하지 않습니다.
- 구조적 결정(대안 검토, 다른 도메인에 영향)을 코드로만 남기고 넘어가지 않습니다.
  `docs/adr/how-to-write-adr.md` §3 기준에 해당하면 ADR을 씁니다 — 미래의 우리는
  지금의 대화를 기억하지 못합니다.
