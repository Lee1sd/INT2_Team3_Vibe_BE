# AI 설계 및 실험 기록 — 커리어 던전 (Vibe)

> 프로젝트 스페이스 가이드 "AI 기록" 요구사항 대응 문서. 최종 프롬프트뿐 아니라 시행착오 과정을 기록합니다.

---

## 1. 사용 모델 및 선택 이유

| 항목 | 내용 |
|---|---|
| 최종 벤더 | Claude (Anthropic) |
| 최종 모델 | Haiku 4.5 (ADR-007 확정) |
| 검토했던 대안 | Gemini(Flash계열), GPT(5.x 중급) |
| 선택 이유 | 루브릭 기반 장문 평가·페르소나 톤 유지에 강점, JSON 출력 안정성 지원, 운영 측 공용 API 키와 월 예산 상한을 사용해 개인 별도 비용이 발생하지 않음 |
| 아키텍처 결정 | LLM 클라이언트를 인터페이스로 추상화하여 벤더 교체 용이하게 설계 (`LlmClient` 인터페이스 + Mock 구현체) |

---

## 2. 프롬프트 설계 과정 — 시행착오 이력

### 2-0. 실제 테스트 로그 (Haiku 4.5 vs Sonnet 4.6)

| 항목 | 내용 |
|---|---|
| 테스트 방식 | Console Workbench에서 동일 Mock 답변으로 Haiku 4.5 vs Sonnet 4.6 비교 |
| 결과 | Haiku 4.5: 총점 4/25, Sonnet 4.6: 총점 2/25 — 판단 방향(부실한 답변→낮은 점수) 일치, feedback 품질도 동등 |
| 발견한 이슈 | Haiku 4.5는 응답을 마크다운 코드펜스(` ```json `)로 감싸서 반환, Sonnet 4.6은 순수 JSON — 코드펜스 스트리핑 방어 로직 필요성 발견(이슈 #8로 등록) |
| 최종 결정 | 채점 방향 일치 + 비용 효율 고려해 Haiku 4.5 채택. 코드펜스 이슈는 실 LLM 연동 시 별도 처리 |

### 2-1. 채점 기준 설계

| 단계 | 내용 |
|---|---|
| **초기안** | 3가지 단순 기준(의도이해/기술근거/트레이드오프)으로 0~25점 총점만 산정 |
| **문제 인식** | 멘토링 피드백: "채점 기준을 항목별로 상세하게 나누고, 일관성 있게 둘 것" |
| **개선 시도** | 기술적정확성(10)/핵심내용충족도(5)/근거판단과정(4)/구체성실무연계(3)/트레이드오프예외(3) = 25점, 각 항목 6단계 점수 구간표 도입 |
| **리스크 검토** | 세부항목이 5개로 늘면서 AI가 한 번에 산정할 숫자가 문항당 5개(4문항×5=20개)로 증가 → JSON 파싱 실패·숫자 누락 리스크 상승 우려 |
| **대응** | JSON 스키마 검증 + 최대 1회 재요청 + 점수 클램핑 방어로직 강화(NFR-05). 실제 Claude API로 2~3회 테스트하여 안정성 확인 후 확정 |
| **API 노출 정책** | 세부점수는 API 응답/화면에 노출하지 않고 총점(score)+종합피드백(feedback)만 제공 — 이유: (1) 프론트 UI 복잡도 증가 방지 (2) 채점 일관성 확보를 위한 내부 로직이지 사용자에게 정밀도를 보장하는 수치가 아니므로 |

### 2-2. 판정 방식 설계

| 단계 | 내용 |
|---|---|
| **검토한 대안 3가지** | ① 누적형(답변마다 채점, 누적평균 80% 이상 해금) ② 단판형(1회 판정) ③ 압박면접형(꼬리질문 3연속) |
| **비교 기준** | 구현난이도, LLM 일관성 리스크, 데모 안정성, 개발공수 |
| **판단** | 누적형은 API 호출 3배 증가+일관성 리스크 큼. 압박면접형은 3연속 판정 모두 일관되어야 해서 리스크 최고. 단판형이 4주 팀 리스크 관리에 가장 적합 |
| **최종 채택** | 단판형 + 보조 꼬리질문 1회 (질문 3개 일괄채점 → 최저점 1개에 타겟 꼬리질문 → 최종 4문항 100점 합산, 80점 이상 통과) |
| **압박면접형 처리** | 완전 폐기하지 않고 스트레치 골로 보류 (팀 정체성/차별점 유지 목적, MVP 완성 후 시간 남으면 도전) |

### 2-3. 질문 생성 방식 설계

| 단계 | 내용 |
|---|---|
| **초기 검토안** | 주제 자유 입력 방식 |
| **문제 인식** | 자유 입력 시 사전 few-shot 예시 준비 불가, 화이트리스트 무력화, 이력서와 무관한 질문 생성 위험 |
| **대안 검토** | "질문 풀에서 사전정의된 질문을 랜덤/가중 선택"하는 방식도 검토했으나 → 서비스 핵심 차별점("범용 질문은행이 아닌 이력서 기반 직접 생성")과 정면 충돌하여 기각 |
| **최종 채택** | 사용자가 6개 키워드(데이터전처리/DB/부하/보안/시스템설계/클라우드) 중 1개를 선택 → 이력서 텍스트 + 키워드를 프롬프트에 동적 주입 → AI가 매번 새로 질문 3개 + 모범답안을 생성. MVP는 검증 부담을 줄이기 위해 키워드 2개(DB, 보안)만 우선 지원 |
| **폴백 처리** | 이력서에 선택 키워드 관련 내용이 부족한 경우, "일반적인 CS 지식 관점에서 질문하라"는 지시를 프롬프트에 포함 |

### 2-3-1. 키워드 반영 경로 확인 (Real LLM vs Mock)

| 항목 | 내용 |
|---|---|
| 확인 배경 | IS-002 설계 논의 중 레이(실무 매니저)가 "질문 키워드가 실제로 반영되는지" 질문 |
| 확인 결과 | Real LLM 경로는 keyword가 요청 → 서비스 → 프롬프트 → 템플릿까지 정확히 반영됨(테스트 검증 완료). MockLlmClient는 keyword와 무관하게 고정 질문 반환 |
| 확인 | 의도된 설계 — Mock은 코드 흐름 검증용이며, 키워드별 내용 차이는 필요 없음. 그대로 유지 |

### 2-4. 페르소나(톤) 설계

| 단계 | 내용 |
|---|---|
| **초기 아이디어** | 기술 성향(스타트업 vs 대기업) 기반 페르소나 — 이후 "톤(널널함/깐깐함)"과 "주제 키워드"로 관심사가 분리되며, 최종적으로는 톤(2종)+키워드(2종) 2개 축으로 정리 |
| **구현 방식** | System Prompt 교체로 구현 (개발 비용 ≈ 0). 실제 대사 문구는 DB가 아닌 파일로 분리 관리(`lenient.txt`, `strict.txt`) — 톤 조정 시 DB 마이그레이션 불필요 |
| **원칙** | 페르소나 수 절대 상한 3개, 기본 2개(널널한 대리/깐깐한 과장)로 MVP 확정 |

### 2-5. 모범답안 생성 시점 조정 및 저장 구조 검증

| 단계 | 내용 |
|---|---|
| **초기 설계 오류** | 요구사항 초안에 "채점 호출 내에서 모범답안 생성, 저장 안 함(휘발성)"으로 기재 |
| **실제 필요 발견** | 채점 로직 개발 중 "모범답안을 서버에 저장해서 채점 시 재사용해야 한다"는 요구사항 확인 (질문생성 LLM과 채점 LLM 호출이 분리되어 있어 저장 필요) |
| **호출 횟수 검증** | 모범답안을 "질문 생성 호출 내에서 함께 생성"하면 질문 생성 영역의 추가 호출을 막을 수 있음을 확인 후 진행. 이후 #59에서 꼬리질문 생성 전용 호출을 추가해 현재 정상 호출 횟수는 세션당 4회로 확정 |
| **최초 저장 구조 확정** | `session_id` 기반 복합키(`session_id`+`question_id`)로 `question_text`까지 함께 저장하는 구조로 확정, 이후 코드리뷰(코덱스) 반영해서 FK/UNIQUE 제약까지 보강(PR #28) |
| **최종 저장 구조 결정** | 이건희와의 재확인 결과, 질문 텍스트 중복 저장을 피하기 위해 `message_id`(FK)로 기존 `messages` 테이블을 참조하는 구조가 맞다고 확정 → `questions` 테이블을 `message_id` 단일 PK/FK + `expected_answer`로 재설계 |
| **문서 반영** | FR-03(생성 시점 저장), FR-04(조회 후 채점), ERD, api-spec(IS-002), open-questions.md #9 정정 완료 |

### 2-6. 채점 계약 정합화 (최초/최종 문항 수 불일치 발견 및 수정)

| 단계 | 내용 |
|---|---|
| 발견 | `judgment` 도메인(③, PR #18)이 이미 "최종 4문항 전체 재채점"을 전제로 설계되어 있었으나, `global.llm`(②) 쪽 실제 요청/응답 계약은 "꼬리질문 1개만 채점하고 turn 번호만 유지"하는 3문항 구조로 남아있음을 발견 |
| 원인 확인 | 기획서 §3 원문("4문항 합산 100점 만점")을 재확인해 "최저점 문항을 대체"가 아니라 "4문항 전체를 유지하며 보강"하는 게 맞는 설계임을 확정 |
| 수정 | `EvaluationRequest`를 3문항(turn 번호만)에서 4문항 전체(질문+답변+모범답안 포함)로 재설계. 이 과정에서 코드리뷰로 turn 중복/범위/누락/weakestQuestionId 처리 등 10라운드 이상의 세부 검증을 거침(ADR-010) |

> 개발 과정에서 발생한 SSOT 검증, 코드리뷰 반영 방어, 도메인 경계와 호출 원칙 충돌 기록은
> [`harness-troubleshooting.md`](harness-troubleshooting.md)로 분리했다.

### 2-10. LLM 재요청 상한 축소 (최대 1회)

| 단계 | 내용 |
|---|---|
| 발생/발견 | LLM 응답 스키마 검증 실패 시 `LlmInvocationService`가 재시도를 과하게 허용하고 있어, 호출 경로별 최악 케이스 호출 수가 예산 대비 커질 수 있음을 확인 |
| 원인/근거 확인 | `LlmInvocationService`의 질문 생성, 최초 채점, 꼬리질문 생성, 최종 채점 `@Retryable` 경로와 관련 재시도 테스트를 확인했고, 운영 문서(`llm-cost-policy.md`, `failure-policy.md`)의 재요청 상한 문구도 같은 정책을 설명하고 있음을 확인 |
| 조치/결정 | `maxAttempts=2`로 줄여 최초 1회 + 재시도 1회만 허용하도록 변경했다. #59 구현 후 현재 코드 기준 정상 호출은 4회, 최악 케이스 호출 횟수는 8회다. 테스트 기대값도 새 재시도 정책에 맞춰 갱신했다 |

### 2-11. 꼬리질문 생성 프롬프트 신규 설계

| 단계 | 내용 |
|---|---|
| 발생/발견 | #59 구현으로 최초채점에서 반환된 최저점 문항을 바탕으로 꼬리질문 1개와 최종 채점용 모범답안을 별도 생성해야 함 |
| 원인/근거 확인 | `evaluateFinalAnswers`는 turn 1~4 전체 재채점을 기대하지만, 기존 구현에는 turn 4 질문과 모범답안을 만드는 LLM 호출이 없었다. 이슈 #59 작업지시서의 입력 계약(`weakestQuestionId`, 원래 질문, 사용자 답변, feedback)과 저장 계약(`messages.role=QUESTION, turn=4`, `questions.message_id + expected_answer`)을 기준으로 확인 |
| 조치/결정 | 기존 질문 생성 프롬프트의 페르소나 system prompt 재사용 구조를 유지하고, user prompt는 원래 질문·부실 답변·feedback에서 빠진 부분을 겨냥하도록 별도 템플릿(`prompts/question-generation/follow-up-user.txt`)으로 작성했다. few-shot 2개를 자체 작성해 포함하고, 출력은 `{ followUpQuestion, expectedAnswer }` 순수 JSON으로 제한했다 |

### 2-12. IS-002 답변 제출/채점 API 부재 발견

| 항목 | 내용 |
|---|---|
| 발견 배경 | 레이(실무 매니저)가 4단계 LLM 호출 전체 흐름(질문생성 → 최초채점 → 꼬리질문생성 → 최종채점)이 다 이어지는지 확인 요청, 이 과정에서 당시 IS-002 엔드포인트 부재를 발견 |
| 정리 | 당시 계획은 이후 IS-002 구현으로 해소됨. 실측 검증은 2-13(End-to-end 실측 검증, IS-002 완성 후 진행)에서 완료됨 |

### 2-13. End-to-end 실측 검증 (IS-001~IS-002 전체 흐름)

| 항목 | 내용 |
|---|---|
| 검증 범위 | 질문생성(a) → 최초채점(b) → 꼬리질문생성(c) → 최종채점(d) 전체 흐름을 실제 Claude API로 끝까지 실행 |
| 호출 횟수 | 4회 (질문생성1, 최초채점1, 꼬리질문생성1, 최종채점1) — 설계대로 정확히 일치 |
| JSON 파싱 안정성 | 전 단계 파싱 성공, 5개 루브릭 필드 검증 통과 |
| 소요 시간 | 약 49.4초 (세션 하나 전체) |
| 상태 전이 | IN_PROGRESS → AWAITING_FOLLOWUP → COMPLETED 정확히 확인 |
| 발견 | 초기 테스트 기대값(최종 응답=turn4 단독)이 실제 API 계약과 달라 1차 실패, 재확인 후 보정하여 재검증 성공 |

### 2-14. raw weakestQuestionId 직접 사용 문제 및 재설계

| 항목 | 내용 |
|---|---|
| 발견 | InterviewService가 최저점 문항(꼬리질문 대상)을 정할 때, ③(judgment)의 보정을 거치지 않고 LLM raw weakestQuestionId를 그대로 사용하고 있었음 |
| 확인 | 최용성 확인 — 비정상 응답 시 raw값과 보정값의 최저점 순위가 달라질 수 있음 |
| 재설계 | ②가 raw evaluations를 ③에 넘기면 → ③이 보정 → 보정된 weakestQuestionId를 ②가 꼬리질문 흐름에 사용 |

### 2-15. 외부 API questionId 식별자 통일 (turn 기준)

| 항목 | 내용 |
|---|---|
| 배경 | 코드래빗이 처음엔 "questionId를 Message.id 기준으로 통일하라"고 지적해 반영했으나, 최용성이 채점 파이프라인 코드를 확인한 결과 turn(1~4) 기준이 더 적합하다고 재확인 |
| 결정 | 외부 API의 questionId는 turn(1~4)으로 통일. 내부적으로는 questions.message_id/Message.id를 통한 매핑을 그대로 유지 |
| 근거 | 채점 로직 전체가 이미 turn 기준으로 설계되어 있어, turn 통일이 변환 로직/버그 위험을 줄임 |

### 2-16. 저장 구조와 API 표기 분리 확정

| 항목 | 내용 |
|---|---|
| 확인 배경 | PM이 "questionId를 turn 기준으로 통일한 것이 questions 테이블 저장 구조(message_id PK/FK) 자체를 바꾼 것인지" 확인 요청 |
| 결론 | 저장 구조(message_id PK/FK)는 그대로 유지. 바뀐 건 API 요청/응답에 노출되는 questionId 값뿐, 내부 매핑(turn→Message 조회→message_id) 로직은 유지됨 |

### 2-17. 최종 채점 응답의 turn 1~4가 서버 병합 결과임을 실측 확인

| 항목 | 내용 |
|---|---|
| 확인 배경 | E2E 검증(2-13) 중 최종 응답에 turn 1~4가 전부 포함된 것을 보고, LLM이 4개를 재채점한 것인지 서버가 합친 것인지 확인 필요 |
| 확인 결과 | LLM은 turn 4만 채점(코드 레벨로 강제, 프롬프트에도 "Score only turn 4" 명시). 서버가 저장된 최초 1~3 점수와 LLM의 turn 4 결과를 합쳐서 외부 API로 응답 |
| 근거 | LlmInvocationService가 최종 요청에 turn 1~3이 섞이면 호출 전에 실패시킴, JudgmentScoringService가 최초 점수 normalize+turn4 합산 |

### 2-18. 채점 프롬프트 v2 런타임 배선

| 항목 | 내용 |
|---|---|
| 발견 | 실제 `ClaudeLlmClient`의 최초·최종 채점 프롬프트는 영문 한 줄 지시와 JSON 스키마만 포함해, 문서에서 검증한 5개 고정 루브릭과 세부 점수 구간표가 실호출에 전달되지 않았다. |
| 범위 조정 | 질문 생성과 동일하게 UTF-8 리소스 템플릿 → 전용 Provider → `InterviewService` → `LlmInvocationService` → `ClaudeLlmClient` 흐름으로 채점 프롬프트를 배선했다. 모델·temperature와 질문 생성 프롬프트는 변경하지 않았다. |
| 계약 반영 | 질문 생성 시 저장한 `expectedAnswer`와 사용자 답변을 비교하고, 최초 turn 1~3과 최종 turn 4 단독 채점의 영문 DTO 스키마를 명시했다. 최초 turn 1~3 확정 평가는 최종 `overallFeedback`용 읽기 전용 컨텍스트로만 사용하도록 고정했다. |
| 검증 | Mock Claude 서버가 받은 실제 요청 본문의 system/user 프롬프트를 검사해 5개 배점·세부 구간표·저장 모범답안·turn 4 단독 채점·영문 JSON 키가 포함되는지 확인했다. 외부 Claude API는 호출하지 않았다. |

### 2-19. 질문생성 few-shot 프롬프트 실 API 재검증 (#89)

| 항목 | 내용 |
|---|---|
| 확인 배경 | 채점 프롬프트는 실제 API 테스트와 E2E 검증 기록이 있었지만, 질문생성 프롬프트는 few-shot 포함 후 반복 검증 기록이 부족했다. FR-03, NFR-10, NFR-12 기준으로 중간점검 전 실제 Claude API 품질을 재확인했다. |
| 테스트 방식 | Claude Haiku 4.5 실제 API로 질문생성 프롬프트를 호출했다. DB 이력서 동일 입력 3회, 보안 이력서 1회, 프론트엔드 중심 이력서+DB 키워드 1회, few-shot 제거 비교 2회, 프롬프트 수정 후 DB/보안/엣지 3회 재검증까지 총 10회 확인했다. |
| 기본 케이스(DB) | DB 이력서에서는 EXPLAIN 지표, 복합 인덱스 컬럼 순서, N+1, 트랜잭션 격리 수준, 재고 차감 동시성 등 이력서 단서에 기반한 질문 3개가 생성됐다. 동일 입력 3회에서도 난이도는 turn 1→3으로 깊어졌고, 핵심 주제 편차는 허용 가능한 수준이었다. |
| 기본 케이스(보안) | 보안 이력서에서는 refresh token hash 저장 이유, token rotation 재사용 탐지, access token 만료/무효화 구분 등 이력서 단서와 보안 키워드를 직접 반영한 질문이 생성됐다. |
| 엣지 케이스(DB 불일치) | 프론트엔드 중심 이력서에 DB 키워드를 넣었을 때, DB 경험을 지어내지는 않고 폼 제출 데이터 영속화, 대량 데이터 페이지네이션/커서 조회, 느린 쿼리 장애 시 프론트-백엔드 협업처럼 일반 CS/DB 지식을 프론트 맥락에 연결한 질문으로 전환됐다. NFR-12 폴백 방향은 동작한다고 판단했다. |
| few-shot 효과 | few-shot 포함 시 질문이 이력서 단서에 더 직접적으로 붙고 turn별 심화 흐름이 안정적이었다. few-shot 제거 시에도 JSON 구조는 안정적이었지만, 일부 엣지 케이스에서 프론트엔드 일반 질문으로 흘러 DB 키워드 반영이 약해지는 결과가 있었다. few-shot은 유지한다. |
| 발견한 문제 | 기존 few-shot 예시 2가 MVP 허용 키워드(DB/보안)가 아닌 `클라우드`였고, 1차 보안 실험 중 Docker/GitHub Actions 같은 예시 주제가 섞일 수 있는 정황이 있었다. |
| 조치 | `prompts/question-generation/user.txt`의 예시 2를 `클라우드`에서 `보안` 키워드 기반 refresh token hash/rotation 예시로 교체했다. 히스토리 스냅샷(`docs/ai/prompts/question-generation-user.txt`)과 프롬프트 조립 테스트 기대값도 함께 갱신했다. |
| 확인 사항 | 보안 키워드 few-shot 예시(클라우드→보안 교체분)가 원래 계획("ksundong 리포 스타일 참고")과 달리, 실제로는 참고 없이 직접 작성됨 (#89/#91에서 확인) |
| 영향 | 검토 범위 내에서는 외부 문구를 그대로 베낀 흔적이 발견되지 않음(스타일 참고 원칙은 지켜지지 않음, 별도 법적/라이선스 검토는 필요 시 진행) |
| 수정 후 재검증 | 수정 후 DB/보안/프론트엔드+DB 엣지 케이스를 실제 Claude API로 다시 호출했다. 세 케이스 모두 `questions` 3개, turn 1~3, `questionText`, `expectedAnswer` 구조를 유지했고, 보안 케이스에서 예시 주제 누수 없이 refresh token 중심 질문이 생성됐다. |
| 남은 리스크 | LLM 특성상 질문 표현의 세부 난이도 편차는 0으로 만들 수 없다. 다만 이번 10회 기준으로 스키마와 주제 반영은 안정적이며, 더 강한 균일성이 필요하면 난이도별 rubric 문구를 별도 추가하는 후속 개선으로 분리한다. |

### 2-20. 엣지케이스 폴백 A/B 검증 및 팀 결정

| 항목 | 내용 |
|---|---|
| 검증 내용 | 진짜 무관 이력서(마케팅/엑셀, 개발 경험 전무)로 DB/보안 재검증 |
| 결과 | 이력서의 비기술 맥락(엑셀 데이터, 설문 응답 등)을 단서 삼아 자연스럽게 DB/보안 일반 개념으로 연결하는 방식으로 동작 (A안: 맥락 연결형 폴백) |
| 팀 결정 | A안(맥락 연결형 폴백) 확정, 코드 변경 불필요 (이미 A로 동작 중) |
| 근거 | 기획서 1장 "이력서 기반 맞춤 질문 생성" 서비스 정체성과 부합. 순수 일반 CS 질문(B안)은 오히려 서비스 차별점(범용 질문은행 아님)과 충돌 |
| 참고 | 기획서 §12 리스크 대응란 문구는 이미 이 결정에 맞게 수정 완료됨 |

### 2-21. 최종 채점 범위 재확정 (turn 4 단독 채점)

| 단계 | 내용 |
|---|---|
| 발생/발견 | 최초 1~3을 최종 LLM에서 다시 채점하면 이미 확정한 점수가 비결정적으로 바뀌고, 최종 호출에서 20개 루브릭 숫자를 다시 생성해야 하는 문제가 확인됐다 |
| 조치/결정 | 최종 LLM의 신규 채점 대상은 turn 4 한 건으로 제한한다. 최초 1~3은 서버 확정 점수를 보존하고, 질문·답변·확정 점수·피드백만 종합 피드백용 읽기 전용 컨텍스트로 전달한다(이슈 #60, ADR-014) |
| 최종 합산 | 서버가 `기존 turn 1~3 확정 점수 + clamp(turn 4)`로 0~100 총점과 80점 이상 합격 여부를 재계산한다. 외부 최종 응답에는 turn 1~4 점수를 모두 포함한다 |

### 2-22. 질문 생성 Haiku 4.5 vs Sonnet 4.6 비교 (동일 조건 1회씩)

실제 Claude API를 2회 호출했다. 조건은 동일하게 고정했다: 같은 이력서, 키워드 `DB`, 페르소나 `STRICT`, userName `지원자`, `QuestionGenerationPromptProvider` 생성 프롬프트 사용. 모델만 `claude-haiku-4-5` / `claude-sonnet-4-6`로 변경했다.

| 항목 | Haiku 4.5 | Sonnet 4.6 |
|---|---|---|
| 질문 1 | 주문 목록 조회에서 `user_id`, `created_at`, `status` 세 컬럼으로 복합 인덱스를 만드셨는데, 이 순서로 정한 이유가 뭔가요? | 주문 목록 조회 쿼리에서 `user_id`, `created_at`, `status`를 기준으로 복합 인덱스를 조정하셨다고 하셨는데, 세 컬럼의 인덱스 순서를 어떻게 결정하셨나요? 그리고 EXPLAIN 결과에서 어떤 지표를 보고 개선 전후를 비교하셨는지 구체적으로 설명해 주세요. |
| 질문 2 | fetch join과 batch size로 N+1 문제를 개선하셨다고 했는데, 두 방식을 어떤 기준으로 선택하셨고 각각 어떤 트레이드오프가 있나요? | N+1 문제를 fetch join과 batch size로 개선하셨다고 하셨는데, 두 방법을 각각 어떤 상황에서 선택하셨나요? fetch join을 적용했을 때 페이징 처리에서 발생하는 문제가 있다면 어떻게 해결하셨는지도 말씀해 주세요. |
| 질문 3 | 재고 차감 로직에서 트랜잭션 격리 수준을 검토하셨다고 했는데, 동시에 같은 상품의 재고를 차감하는 상황에서 어떤 격리 수준을 선택했고, 그 이유는 뭔가요? 선택하지 않은 다른 수준의 문제점도 설명해 주세요. | 재고 차감 로직에서 트랜잭션 격리 수준과 동시성 충돌을 검토하셨다고 하셨는데, 구체적으로 어떤 격리 수준을 선택하셨고 그 이유는 무엇인가요? 낙관적 락과 비관적 락 중 어떤 방식을 적용하셨는지, 그리고 선택하지 않은 방식의 트레이드오프는 무엇인지 설명해 주세요. |

| 기준 | Haiku 4.5 | Sonnet 4.6 |
|---|---|---|
| 이력서 관련성 | 이력서의 복합 인덱스, N+1, 재고 차감/격리 수준을 모두 반영함. | 동일하게 반영하면서 EXPLAIN 지표, 페이징 문제, 락 전략까지 이력서 맥락을 더 확장함. |
| 구체성 | 핵심 경험을 직접 묻지만 질문 폭은 비교적 짧고 일반 면접 질문에 가까움. | 더 구체적임. `EXPLAIN` 지표, fetch join 페이징 문제, 낙관/비관 락 트레이드오프까지 파고듦. |
| 톤 | STRICT 톤이 어느 정도 반영되어 이유와 트레이드오프를 요구함. | STRICT 톤이 더 강함. "구체적으로 설명", "선택하지 않은 방식의 트레이드오프"처럼 검증 압박이 더 뚜렷함. |

| 항목 | 내용 |
|---|---|
| 판단 | Sonnet이 이력서 맥락 확장/구체성/톤 강도에서 근소하게 우위. 단일 케이스 비교라 통계적 유의성은 없음 |

### 2-24. IS-002 실 API 재검증 (7/22~7/23)

| 항목 | 내용 |
|---|---|
| 검증 배경 | `ClaudeLlmClient` 생성자 주입 버그 확인 및 DB/보안 키워드별 질문 품질 비교 |
| 호출 내역 | 세션12/13(N+1 이력서, 생성자 버그 확인+세션12 완료 진행): 5회(세션12 완료 플로우 4회 + 버그 확인/재진행 1회) / 세션22(Lv.1 DB, 정상 완료 플로우): 4회 / 세션23(Lv.2 보안, 정상 완료 플로우): 4회 |
| 총 호출 | 13회(세션12/13 5회 + 세션22 4회 + 세션23 4회, 정상 완료 플로우 기준은 세션당 4회이며 재요청은 단계별 `@Retryable(maxAttempts=2)`로 1회까지 허용) (Claude Haiku 4.5) |
| 발견 | 1) 이력서에 보안 경험 없어도 일반 CS 폴백 정상 작동 확인, 2) 종료 멘트에 `overallFeedback` 중복 노출 문제 발견 및 수정, 3) 채점 피드백 길이/페르소나 톤/레벨별 난이도 체감 이슈 발견 |
| 후속 | 페르소나 톤 few-shot 강화, 주니어 이력서 난이도 재검증 예정 |

### 2-25. lenient 페르소나 few-shot 어미 정중화 실 API 재검증 (#140)

| 항목 | 내용 |
|---|---|
| 확인 배경 | `lenient.txt` few-shot 예시2의 "~시겠어요?" 어미를 "~시겠나요?"로 통일한 뒤, 실제 Claude API 호출에서도 정중한 의문형 어미가 반영되는지 확인 |
| 테스트 방식 | Claude Haiku 4.5 실제 API로 `IS-001`(면접 세션 생성) 1회 호출. LENIENT 페르소나, 키워드 `DB`, DB 인덱스/N+1/트랜잭션 격리 경험이 담긴 이력서 사용 |
| 결과 | 생성된 질문 3개 모두 "~나요?" 계열 정중 의문형으로 끝남: "...이유가 뭔가요?", "...선택하시나요?", "...방지하셨나요?". "~어요?" 형태는 나타나지 않음 |
| 판단 | few-shot 어미 수정이 실제 생성 결과에 반영됨을 확인. 단일 호출 기준이라 반복 검증은 아니며, 추후 다회 호출로 재확인 여지 있음 |
| 후속 조치 | 테스트용으로 로컬 DB에 심었던 임시 유저/이력서/세션(user 9002 등)은 검증 직후 삭제. `application-local.yml`은 mock 모드로 복귀, real API 키는 제거 |

### 2-26. 주니어 이력서 난이도 재검증

| 항목 | 내용 |
|---|---|
| 검증 내용 | 신입~1년차 수준 이력서(할일 관리 CRUD, 공지사항 게시판)로 Lv.1/Lv.2 각각 질문 생성 |
| 호출 수 | 2회 (Lv.1 1회, Lv.2 1회) |
| 결과 | 둘 다 시니어급(트레이드오프/아키텍처) 질문 없음, Lv.2가 Lv.1보다 약간 더 구체적 |

### 2-27. 페르소나 톤 few-shot 초기 검증

| 항목 | 내용 |
|---|---|
| 검증 내용 | few-shot 반영 후 lenient/strict 톤 차이 확인 (반갑습니다~/시작하겠습니다 등) |
| 호출 수 | 2회 (lenient 1회, strict 1회) |
| 결과 | 톤 차이 명확히 확인됨, few-shot이 문체 방향을 유도하는 형태로 반영 |

### 2-28. 5문항·문항당 20점 채점 계약 반영 (#147)

| 항목 | 내용 |
|---|---|
| 변경 배경 | 최초 질문이 4개로 확장되어 꼬리질문 1개를 포함한 다섯 문항 합계가 기존 100점 계약을 유지하도록 배점을 재분배할 필요가 생김 |
| 프롬프트 변경 | 기술적 정확성 8점, 핵심 내용 충족도 4점, 근거·판단 과정 3점, 구체성·실무 연계 3점, 트레이드오프·예외 2점으로 변경하고 세부 점수 구간을 20점 기준으로 조정 |
| 서버 방어 | 각 루브릭을 8/4/3/3/2 상한으로 clamp하고 문항 점수를 0~20, 총점을 0~100으로 제한. 최종 LLM은 turn 5만 채점하고 최초 turn 1~4 서버 확정 점수를 재사용 |
| 합격 기준 | Lv.1은 60점, Lv.2는 80점. LLM이 제공한 `totalScore`와 `passed`는 신뢰하지 않고 서버가 세션 레벨 정책으로 재계산 |
| 검증 범위 | Mock 기반 전체 테스트 스위트 421건 중 417건 통과, 3건 실패(모두 `IS-002`/`IS-002b` 실 흐름 통합테스트). 실패 원인은 `#147`(PR #151)이 아직 안 끝나서가 아니라 — PR #151은 이미 main에 머지 완료 — 새 배점(Lv.1 60점 통과선)으로 이 테스트들이 처음으로 Lv.1을 통과하게 되면서 `BadgeAwardService`가 Stage2 뱃지를 찾지 못해 `BadgeNotFoundException`을 던지는 별도 회귀임(progress 도메인, 최용성 확인 필요). 실제 LLM 호출은 수행하지 않았으며 변경된 프롬프트의 실 API 품질 검증은 LLM owner 후속 범위 |

### 2-29. 모델비교 Phase 1 파일럿 — 이력서 2개 × Haiku 4.5/Sonnet 4.6 × Lv.1/Lv.2 (질문 생성)

`모델비교_테스트_작업지시서.md` Phase 1의 축소판. 이력서 10개 전체가 아니라 이력서 1(커머스,
DB 풍부)·2(채팅, 보안 풍부) 2개만 선정해 예산/시간 부담을 줄인 파일럿.

| 항목 | 내용 |
|---|---|
| 호출 구성 | 이력서 2개 × 모델 2개(Haiku 4.5, Sonnet 4.6) × 레벨 2개(Lv.1, Lv.2) = 성공 8건 |
| 실 API 호출 총 시도 | 10회 (성공 8, 실패 2 — 아래 "실패 상세" 참고). 모델은 `application-local.yml`의 `llm.model`이 부팅 시 고정 주입되는 구조라 배치 사이에 `bootRun` 재시작 필요 |
| 실패 상세 | Sonnet에서만 2건 실패 후 재시도 성공: ①`SocketTimeoutException`(read-timeout 30s 초과, provider 응답 자체를 못 받음 — 60s로 늘려 재시도 성공) ②`JsonEOFException`(max-tokens 2048에서 응답이 중간에 잘림 — 4096으로 늘려 재시도 성공). Haiku는 4건 모두 1회 성공 |
| 이력서 관련성 | 두 모델 모두 이력서의 구체적 기술 결정(SELECT FOR UPDATE, Redis+Lua, 커서 페이지네이션, WebSocket 헤더, XSS 이중방어, Rate Limiting 등)을 정확히 짚어 질문함. 관련성 자체는 두 모델 다 우수 |
| 레벨(Lv.1/Lv.2) 난이도 격차 | **Haiku**는 Lv.1(직접 설명 요구)→Lv.2(엣지케이스·정합성·근거까지 요구)로 격차가 뚜렷함. **Sonnet**은 Lv.1 시점부터 이미 내부 메커니즘·트레이드오프 수준 질문을 던져서, Lv.2로 올라가도 체감 격차가 Haiku보다 작음 — 이력서 관련성(둘 다 "풍부" 등급)과 무관하게 두 이력서 모두 동일한 패턴 |
| JSON 파싱 | Haiku: 4/4 1회 성공, 이슈 없음. Sonnet: 스키마 이탈(코드펜스 등)은 없었으나, 응답이 길어 타임아웃/토큰상한에 더 자주 걸림 — 순수 JSON 파싱 실패는 아니지만 인프라 설정값이 Sonnet 기준에는 타이트함을 시사 |
| 판단 | 1차 거부조건(모델_선정_기준.md) 관점: 두 모델 다 탈락 사유 없음(JSON 자체는 결국 둘 다 정상 파싱됨). 다만 Sonnet 응답이 더 길어 현재 타임아웃(30s)/토큰상한(2048)으로는 재시도가 잦을 수 있음 — Sonnet 부분 채택 시 해당 단계만 타임아웃/토큰상한을 늘리는 별도 설정이 필요할 것으로 보임. 이력서 10개 전체·Phase 2는 아직 미실행 |
| 사후 조치 | 임시 유저 2명(9003, 9004)·이력서 2건·세션 8건은 검증 직후 `users` 삭제로 cascade 정리. `application-local.yml`은 mock 모드로 복귀, real API 키는 그대로 두되(비커밋 파일) 블록은 주석 처리, read-timeout/max-tokens는 테스트 전 원래 값(30s/2048)으로 복귀 |

### 2-30. 4+1 종합 최종 커리어 리포트 강화 (#156)

| 항목 | 내용 |
|---|---|
| 변경 배경 | 기존 `overallFeedback`이 2~4문장에 그쳐 지원자의 기술 선택 근거, 현업 관점 성장 포인트, 다음 면접에서 사용할 정량 답변 예시를 충분히 전달하지 못함 |
| 프롬프트 변경 | 최종 피드백을 `🎯 총평`/`✨ 이런 점이 매우 훌륭했어요`/`🚀 합격을 확정 짓는 2%`/`💡 Next Step`의 Markdown 리포트로 고정하고, Transcript에 나온 기술을 사용한 `❌ AS-IS`/`⭕ TO-BE` 비교를 추가 |
| 4+1 경계 | 최종 LLM은 turn 5만 8/4/3/3/2 루브릭으로 채점하고, 최초 turn 1~4의 질문·답변·서버 확정 점수·피드백은 재채점 없이 최종 리포트용 읽기 전용 컨텍스트로 사용 |
| 계약 보정 | `LlmInvocationService`와 `MockLlmClient`에 남아 있던 이전 평가 0~25 허용 및 Mock 10/5/4/3/3 배점을 0~20·8/4/3/3/2로 동기화 |
| 검증 방식 | 리소스 프롬프트 조립 테스트, Mock LLM 응답 형식 테스트, IS-002b 컨트롤러 통합 테스트에서 5개 평가와 최종 Markdown 섹션을 확인. 실제 Claude API는 호출하지 않음 |

### 2-31. 4+1 최종 커리어 리포트 Claude 실 API 검증 (#156)

| 항목 | 내용 |
|---|---|
| 검증 시나리오 | 모놀리식 Spring Boot 환경의 N+1, 읽기·쓰기 트랜잭션 분리, Redis cache-aside 경험을 입력하고 질문 생성 → 최초 4문항 채점 → 최저점 꼬리질문 생성 → turn 5 최종 채점·종합 피드백까지 실제 애플리케이션 경로로 실행 |
| 호출 수 | Claude Haiku 4.5 총 6회. 전체 E2E 4회와 최종 프롬프트 교정 재검증 2회이며 모든 호출의 스키마 재시도는 0회 |
| 최초 서버 확정 | turn 1~4 점수 16/17/15/5, 합계 53점. 최저점 turn 4를 기준으로 JPA 1차 캐시와 Redis 다중 인스턴스 정합성 꼬리질문을 생성 |
| 최종 서버 확정 | turn 5는 16점, 최초 확정 점수와 합산한 5문항 총점은 69점, Lv.2 기준 80점 미만으로 불합격. 최초 turn 1~4 점수와 피드백은 변경되지 않음 |
| 1차 발견 | TO-BE에 Transcript에 없던 수치를 실제 모니터링 결과처럼 단정함. 가상 수치 고지와 모든 예시 값의 `예:` 표지를 프롬프트에 강제 |
| 2차 발견 | 고지가 TO-BE 예시 뒤로 이동하고 `모범답안`, `turn` 같은 내부 용어와 추가 소제목이 노출됨. 고지 위치, 내부 용어 금지, 4개 섹션 외 제목 금지를 추가 |
| 최종 결과 | TO-BE 제목 바로 다음 줄에 가상 수치 고지가 출력되고 모든 정량 값이 예시로 표시됨. 총평·강점 2개·성장 포인트·AS-IS/TO-BE가 답변 기술과 자연스럽게 연결됐으며 내부 처리 용어와 추가 섹션은 노출되지 않음 |
| 리뷰 후 런타임 방어 | 네 섹션 순서, Strengths 불릿 2개, AS-IS/TO-BE, 가상 수치 고지·표지, 내부 용어를 서버에서 검증하고 형식 이탈 시 기존 스키마 오류와 동일하게 최대 1회 재요청하도록 보강 |
| 리뷰 후 실호출 재검증 | `ClaudeCareerReportRealApiTest`로 2회 실행했으나 두 요청 모두 Anthropic HTTP 401로 추론 전에 거부되어 새 출력은 생성되지 않음. 첫 실행에서 비재시도 공급자 오류가 `ExhaustedRetryException`으로 바뀌는 기존 문제를 발견했고, `notRecoverable`로 원인 예외와 상태 코드가 그대로 전달되도록 수정한 뒤 두 번째 실행에서 `LlmProviderConfigException(status=401)` 직접 전파를 확인. 유효한 공용 키 갱신 후 동일 테스트 재실행 필요 |

### 2-32. 모델비교 Phase 2 최소 범위 — Sonnet 단계별 타임아웃/잘림 검증

`모델비교_테스트_작업지시서.md` Phase 2의 축소판. 이력서 3개 전체가 아니라 이력서 1(커머스,
DB)만으로 최초채점·꼬리질문생성·최종판정 3단계를 기존 타임아웃(30s/2048)에서 검증.

| 항목 | 내용 |
|---|---|
| 1차 시도 — 질문생성 포함 전체 흐름 | 컨트롤러 경로(세션 생성)로 시도. 질문생성(1단계) 자체가 `SocketTimeoutException`으로 4회 연속 실패해 2~4단계(최초채점/꼬리질문생성/최종판정)를 관찰하지 못함(#2-29와 동일 원인) |
| 방식 전환 | DB 시딩 대신 `ClaudeCareerReportRealApiTest`와 같은 패턴으로 컨트롤러/DB 없이 `LlmInvocationService`+실제 `ClaudeLlmClient`만으로 각 단계를 독립 호출(`ClaudeInitialAndFollowUpRealApiTest` 신규 추가, #161). 질문+답변은 Phase 1(#2-29)에서 실제 Haiku가 생성한 이력서1 질문 4개를 그대로 재사용 |
| 최초채점 단독 호출 | Sonnet, 17.1초에 완료. 타임아웃/잘림 없음. 총점 43, weakestQuestionId=2(의도적으로 부실하게 쓴 문항을 정확히 지목) |
| 꼬리질문생성 단독 호출 | Sonnet, 13.0초에 완료. 타임아웃/잘림 없음 |
| 최종판정 단독 호출 | 기존 `ClaudeCareerReportRealApiTest`를 Sonnet으로 재실행 — 재시도 2회 모두 `SocketTimeoutException`(Read timed out)으로 실패, `LlmPermanentFailureException`으로 종결. 질문생성과 같은 실패 양상 |
| 판단 | Sonnet의 타임아웃 문제는 "전 단계 공통"이 아니라 **응답이 긴 단계(①질문생성, ④최종판정/커리어 리포트)에 집중**됨. 응답이 짧은 ②최초채점·③꼬리질문생성은 기존 30s/2048 그대로도 문제없음. Phase 1의 "Sonnet은 60s/4096 필요"라는 결론은 ①·④ 단계에 한정해서 적용해야 정확함 |
| 사후 조치 | DB 시딩 없이 순수 테스트 계층 호출이라 정리할 임시 데이터 없음. `application-local.yml`은 mock 모드로 복귀, real 블록 주석 처리, API 키는 삭제(빈 값) |

### 2-33. 채점 프롬프트 v3(8/4/3/3/2, 0~20) 실 API 개별 재검증 — #2-31 401 후속 확인

`#2-31`이 남긴 "유효한 공용 키 갱신 후 동일 테스트 재실행 필요" 후속. 최초 채점(IS-002)
엔드포인트 단독으로 실 Claude를 1회 호출해 v3 루브릭이 실제로 0~20 범위로 동작하는지 확인.

| 항목 | 내용 |
|---|---|
| 사전 확인 | `prompts/scoring/system.txt`, `docs/ai/prompts/scoring-prompt-v2.md`, `ScoringPromptProviderTest`/`ClaudeLlmClientScoringPromptTest`/`MockLlmClient.java` 전부 main 기준 이미 8/4/3/3/2·0~20으로 반영되어 있음을 코드로 직접 확인(추가 수정 불필요) |
| 호출 수 | Claude Haiku 4.5 1회. 사전에 저장된 질문 4개(turn 1~4, 커머스/DB 이력서)에 3개는 준수한 답변, 1개(turn 2)는 의도적으로 부실한 답변을 제출 |
| 결과 | turn별 점수 18/2/17/16, 합계 53(4개 항목 합과 정확히 일치). 이번 호출의 값은 전부 0~20 범위 안에 있었음(옛 0~25 스케일이 아니라는 확정 근거는 위 "사전 확인"의 코드 검증) |
| weakestQuestionId | 2 — 의도적으로 부실하게 쓴 turn 2를 정확히 최저점으로 지목하고 그 문항을 겨냥한 꼬리질문을 생성함 |
| 공용 키 상태 | 이번 호출은 200으로 정상 처리됐고 401이 재현되지 않았음(`#2-31` 보고 대비) |
| 사후 조치 | 임시 유저(9005)·이력서·세션은 검증 직후 `users` 삭제로 cascade 정리. `application-local.yml`은 mock 모드로 복귀, real 블록은 주석 처리 |

### 2-34. #167 리포트/점수 분리 실 API 재검증 — 3회 연속 fallback

`#167`(CareerReportValidator 실패 시 점수까지 유실되던 버그) 수정 후, 실제 Claude로 최종판정을
3회 호출해 "리포트 콘텐츠 검증이 실패해도 totalScore/passed는 항상 보존되고, 리포트는 안전한
대체 문구로 바뀌는지" 재검증.

| 항목 | 내용 |
|---|---|
| 방식 전환 | `ClaudeCareerReportRealApiTest`(Spring 컨텍스트 기반)로 3회 시도했으나 매번 2분 이상 stdout 출력 없이 멈춰(hang) 강제 종료. Gradle daemon/Spring 컨텍스트 부팅 경로 자체가 원인으로 의심돼, Spring 없이 `ClaudeLlmClient`+`LlmResponseValidator`+`LlmInvocationService`를 직접 `new`로 구성해 1회 호출하는 순수 자바 스모크 스크립트(`RealApiSmokeMain`, 검증 후 삭제)로 전환. 이후 3회 모두 수 초 내 정상 완료 — Gradle test 태스크 경로가 원인이었을 가능성이 높음(하네스 트러블슈팅으로 별도 기록 필요) |
| 호출 수 | Claude Haiku 4.5, 최종판정(IS-002b) 단독 호출 3회 (`ClaudeCareerReportRealApiTest`와 동일한 turn 1~4 컨텍스트 + turn 5 답변 고정 입력) |
| 결과 | 3회 모두 `CareerReportValidator`의 콘텐츠 규칙(금지어/가상수치 표지 등)에 걸려 `FALLBACK_REPORT`로 대체됨. 반면 `totalScore`(11/12/11)와 `passed`(3회 모두 false)는 매번 정상 반환 — 예외로 전체가 죽지 않음을 확인 |
| 판단 | #167의 핵심 목적(리포트 실패와 무관하게 점수 보존)은 3/3 확인 완료. 다만 **3회 모두 fallback이 발동했다는 것은 콘텐츠 규칙 자체를 실제로 통과하는 사례를 아직 한 번도 관찰하지 못했다는 뜻** — `CareerReportValidator`가 지나치게 엄격하거나, 최종판정 프롬프트가 금지어/가상수치 표지 규칙을 안정적으로 지키도록 충분히 강화되지 않았을 가능성이 있음. 후속 검토 필요(프롬프트 재보강 또는 규칙 완화) |
| 사후 조치 | `RealApiSmokeMain.java`, 임시 classpath/init-script 산출물은 검증 직후 삭제. `application-local.yml`은 mock 모드로 복귀, real 블록 주석 처리, API 키는 비움. `ClaudeCareerReportRealApiTest`의 assertion은 #167 계약(리포트가 유효 4섹션이거나 정확히 `FALLBACK_REPORT`)에 맞춰 영구 반영 |

### 2-35. 최종판정 프롬프트에서 가상 수치 고지 지시 제거 — 중복 노출 여부 재검증

`final-user.txt`가 모델에게 가상 수치 고지 문구(`※ 아래 수치는...`)를 TO-BE 바로
다음 줄에 직접 쓰라고 지시하고 있었는데, 서버가 `appendHypotheticalDisclaimer()`로
이미 항상 덧붙이고 있어(#165) 모델이 같은 문구를 스스로도 쓰면 최종 리포트에
고지가 두 번 노출될 수 있는 잠재 결함이었다(PM 지시로 발견 및 수정).

| 항목 | 내용 |
|---|---|
| 수정 | `prompts/scoring/final-user.txt`에서 "TO-BE 바로 아래 첫 줄에 고지를 그대로 출력하라"는 지시와 JSON 예시 스키마에 박혀 있던 고지 문구를 제거. `[예: ...]` 표지 지시는 유지(서버가 자동 생성하지 않는 부분이라 여전히 필요) |
| 재검증 방식 | Spring 컨텍스트 없이 `LlmClient`를 직접 호출해 **서버 처리 전 원본 모델 응답**과 `LlmInvocationService`를 통과한 **최종 응답**을 나란히 비교하는 경량 스모크 스크립트(`RealApiSmokeMain`, 검증 후 삭제)로 2회 실행 |
| 호출 수 | Claude Haiku 4.5, 최종판정(IS-002b) 원본+서비스경유 각 1회씩 총 2회 × 2라운드 = 4회 |
| 결과 | 2회 모두 **원본 모델 응답에 "가상" 단어가 단 한 번도 등장하지 않음**(문자열 카운트 0/0) — 고지 지시를 지워도 모델이 스스로 비슷한 문구를 쓰지 않는다. 1라운드는 우연히 콘텐츠 검증을 통과해(이번 세션 최초의 실제 통과 사례) 최종 응답에서 고지가 정확히 1회만 등장함을 직접 확인(`CLAUDE_DISCLAIMER_OCCURRENCE_COUNT=1`) — 중복 없음 |
| 부가 관찰 | `[예: ...]` 표지 지시는 그대로 남겨뒀는데도 두 원본 응답 모두 TO-BE 정량값에 표지를 정상적으로 붙임 — 고지 지시 제거가 이 규칙 준수에 영향 없음 |
| 사후 조치 | `RealApiSmokeMain.java` 삭제. `application-local.yml`은 mock 모드로 복귀, real 블록은 주석 처리하되 **API 키는 이번엔 삭제하지 않고 유지**(팀 지시로 변경 — 다음 재검증 때 재입력 수고를 덜기 위함). 전체 테스트 446개 재실행, 0 실패 확인 |

### 2-36. 리포트 콘텐츠 검증 실패 시 1회 재요청 추가 — 실 API로 재요청/fallback 두 경로 확인

`failure-policy.md` §2 "형식 이탈 시 최대 1회 재요청" 정책이 리포트 콘텐츠 검증 실패
경로에는 빠져 있던 걸 PM이 지적, `LlmInvocationService.withSanitizedReport()`에 1회
재요청을 추가한 뒤(재요청 성공 시 재요청 리포트 사용, 실패 시 `FALLBACK_REPORT`, 점수는
항상 최초 응답 값 유지) 실 API로 재검증.

| 항목 | 내용 |
|---|---|
| 검증 방식 | `LlmClient`를 호출 횟수를 세는 카운팅 래퍼로 감싸, `evaluateFinalAnswers` 실제 호출
  횟수를 직접 관찰(`RealApiSmokeMain`, 검증 후 삭제) |
| 호출 수 | Claude Haiku 4.5, 최종판정(IS-002b) 2회 실행 — 1회차 1콜(재요청 불필요) + 2회차 2콜(재요청
  발생) = 총 3콜 |
| 결과 1회차 | 리포트가 첫 시도에 검증 통과 — 재요청 없이 `CALL_COUNT=1`, 정상 리포트 + 가상 수치
  고지 확인 |
| 결과 2회차 | 1차 실패("가상 수치 [예: ...] 미표시") → 로그 "1회 재요청합니다" → 재요청 → 2차도
  실패("내부 처리 용어 turn 노출") → 로그 "재요청도 검증 실패, 안전한 대체 문구로 대체합니다"
  → `CALL_COUNT=2`, `FALLBACK_REPORT`, 점수(11, passed=false)는 1차 응답 값 그대로 보존 |
| 판단 | 재요청 로직이 의도대로 정확히 1회만 동작하고, 점수가 재요청 여부와 무관하게 최초
  응답 값으로 고정됨을 실 API로 직접 확인. self-invocation 문제로 `@Retryable` 대신 수동
  재요청으로 구현한 선택이 실제로도 정확히 1회로 제한됨을 확인(무한 재요청 등 이상 동작 없음) |
| 사후 조치 | `RealApiSmokeMain.java` 삭제. `application-local.yml`은 mock 모드로 복귀, real 블록
  주석 처리, API 키는 유지(팀 지시). 전체 테스트 450개 재실행, 0 실패 확인 |

### 2-37. #164 Lv.2(과장) 채점 밸런스 검증 — 준수한 수준 답변 80점 커트라인 미달

`docs/ai/test-resumes/AI생성_이력서_6종.md` 이력서 1(커머스, DB 경험 풍부)로 Lv.2
(interviewerId=2) 실제 세션을 실 Claude API로 진행. 이력서 텍스트는 DB에 DONE 상태로
직접 시딩(S3 프리사인드 업로드 플로우는 검증 대상이 아니므로 우회), 실제 애플리케이션
(bootRun)을 real 모드로 띄우고 REST API로 최초 4문항 + 꼬리질문 1문항을 "준수한 수준"
(핵심 개념·트레이드오프는 언급하되 정량적 수치나 극단적 엣지케이스까지는 다루지 않는
수준)으로 답변 제출.

| 항목 | 내용 |
|---|---|
| 세션 1 (조정 전) | 최초 4문항 18/15/14/12=59점, weakestQuestionId=4, 꼬리질문(Redis/MySQL
  정합성 감지 설계) 17점 → **최종 76점, 80점 미달**. feedback 5건 전부에서 "정량적 근거·수치
  부족", "동시성·엣지케이스 설계 부족" 지적이 공통적으로 반복됨 |
| 원인 분석 | `prompts/scoring/system.txt`의 구간 기준 확인 — `specificity`(구체성·실무연계)
  3점 기준이 "수치 또는 결과"를 사실상 요구하는 문구였고, `tradeOffsAndExceptions`
  (트레이드오프·예외) 2점 기준도 "장단점, 대안, 예외 또는 장애 상황까지"라는 문구가
  4가지를 전부 다뤄야 하는 것처럼 읽혀 과도하게 엄격하게 해석될 여지가 있었음 |
  | 1차 조정 | PM 지시대로 `system.txt` 두 항목의 3점/2점 구간 문구를 완화: specificity는
  수치 없이도 구현 로직·데이터 흐름이 명확하면 만점 가능하도록, tradeOffsAndExceptions는
  "네 가지 중 최소 하나"만 다뤄도 만점이라고 명시 |
| 세션 2 (조정 후) | 새 세션으로 동일 이력서·키워드 재진행. 최초 4문항 18/17/13/14=62점(세션
  1 대비 +3), weakestQuestionId=3, 꼬리질문(청크 멱등성 설계) → **최종 75점, 여전히 80점
  미달**. 조정으로 이론상 도달 가능 최댓값은 79→82로 늘었지만(총점 상한이 초기 4문항 합에
  달려 있어서), 실제 최종 점수는 세션 1과 사실상 동률(76→75, 잡음 수준 차이) |
| 판단 | n=2 표본으로는 1차 관대화가 뚜렷한 개선을 만들지 못했다. 표본이 매우 적어(이력서
  1종·세션 2회) 결론을 확정하기는 이르며, 추가 세션 반복이나 2차 조정 폭 확대(예: 다른
  루브릭 항목도 재검토, 또는 문항 수/배점 자체를 ③ 영역에서 재검토)가 필요할 수 있음.
  이번 라운드에서는 여기까지만 진행하고 결과를 그대로 기록 — 추가 조정은 팀 판단 필요 |
| 부가 확인 | 두 세션 모두 최종 리포트가 `overallFeedback` 4섹션 정상 생성 또는
  `FALLBACK_REPORT`로 안전하게 대체됨(세션 2는 fallback) — #167/#168의 점수 보존·재요청
  로직이 실제 전체 애플리케이션 플로우(리포트 업로드 우회 제외한 나머지는 실제 컨트롤러·
  DB 경유)에서도 정상 동작함을 추가로 확인 |
| 사후 조치 | 임시 유저(9008)·이력서·세션은 검증 직후 `users` DELETE로 cascade 정리 확인
  (judgment_results까지 삭제됨을 재조회로 검증). 시딩/조회/정리용 임시 자바 스크립트 5개
  전부 삭제. `application-local.yml`은 mock 모드로 복귀, real 블록 주석 처리, API 키는 유지
  (팀 지시). bootRun 프로세스 종료, gradle daemon 정리. 전체 테스트 451개 재실행, 0 실패 확인 |

### 2-38. #164 세부 루브릭 점수(5개 항목) 실측 확인 — 임시 진단용 로그 추가 후 제거함

#2-37에서 문항별 합산 점수와 feedback 텍스트만으로 "specificity/tradeOffsAndExceptions가
낮을 것"이라 추정했으나, API 응답도 DB도 5개 세부 루브릭 점수(technicalAccuracy/
coreCoverage/reasoning/specificity/tradeOffsAndExceptions)를 노출·저장하지 않아
직접 확인이 불가능했다(의도된 정책 — "세부점수는 API 응답/화면에 노출하지 않는다").
PM 지시로 **임시 진단용 로그**를 `LlmResponseValidator.validateEvaluationCore()`에
한 줄 추가해(서버 콘솔/로그 파일에만 출력, API 응답·DB에는 전혀 영향 없음 — ③ judgment
도메인 코드는 건드리지 않음) 원시 값을 직접 확인한 뒤, **확인 즉시 제거**했다
(`git diff HEAD`로 파일이 커밋 시점과 완전히 동일함을 확인).

| 항목 | 내용 |
|---|---|
| 세션 3(세부점수 캡처) | 이력서 1(커머스)로 새 세션 진행, 초기 4문항 17/16/13/13=59점 +
  꼬리질문 11점 = **최종 70점**, 여전히 80점 미달 |
| turn별 세부점수 | turn1: tech7/8, core4/4, reason3/3, spec2/3, trade1/2 (score17) · turn2:
  tech6/8, core3/4, reason2/3, spec3/3, trade2/2 (score16) · turn3: tech5/8, core2/4,
  reason2/3, spec2/3, trade2/2 (score13) · turn4: tech5/8, core2/4, reason2/3, spec2/3,
  trade**3**/2(범위 초과, raw score=14) · turn5(꼬리): tech4/8, core3/4, reason2/3, spec1/3,
  trade1/2 (score11) |
| 항목별 총점 대비 % | technicalAccuracy 27/40=67.5% · coreCoverage 14/20=70% ·
  reasoning 11/15=73.3% · specificity 10/15=66.7% · tradeOffsAndExceptions(clamp 반영)
  8/10=80% |
| **가설 정정** | #2-37의 feedback 텍스트 기반 추정과 실제 수치가 다르다 — **가장 낮은
  건 tradeOffsAndExceptions가 아니라 technicalAccuracy(67.5%)와 specificity(66.7%)**다.
  tradeOffsAndExceptions는 오히려 5개 항목 중 가장 높다(80%). feedback 문장에 "정량적
  근거 부족"이 자주 등장해서 specificity 관련 지적처럼 읽혔지만, 실은 technicalAccuracy
  (세부 설명의 정확성)도 비슷한 수준으로 깎이고 있었다 |
| 부가 발견 | turn4에서 LLM이 tradeOffsAndExceptions=3(허용 범위 0~2 초과)을 반환했고,
  raw 합산 score=14였지만 실제 반환된 score는 13 — 서버가 범위 초과 값을 clamp해
  score를 13으로 보정한 것으로 보임(③ judgment 도메인의 clamp 방어가 실제로 작동 중임을
  실측으로 확인, 별도 버그는 아님) |
| 판단 | n=1(세부점수 기준)이라 확정적 결론은 이르지만, #164 2차 조정을 한다면
  technicalAccuracy/specificity 구간 기준도 함께 재검토가 필요할 수 있음 — tradeOffsAndExceptions만
  더 완화하는 방향은 실측 데이터와 맞지 않을 수 있다 |
| 사후 조치 | 임시 진단 로그(`[TEMP-DIAG-#164]` 태그) 확인 직후 제거, `git diff HEAD`로
  원상 복구 확인. 시딩/정리용 임시 자바 스크립트 4개 삭제. 임시 유저(9009) cascade 삭제.
  `application-local.yml` mock 복귀, real 블록 주석 처리, API 키 유지. bootRun/gradle
  daemon 종료. 전체 테스트 451개 재실행, 0 실패 확인 |

### 2-39. 세부점수 밸런스패치 — 짐작 vs 실측 검증 사례

> **발표 트러블슈팅 섹션 후보**

| 항목 | 내용 |
|---|---|
| 초기 판단 | feedback 텍스트만 보고 tradeOffsAndExceptions(#2-36)와 specificity를 동시에 완화 (원문 대조 검증 없음) |
| 실측 결과 | (#2-38) tradeOffsAndExceptions는 실제로 최고점(80%)이었음 — 짐작이 완전히 틀렸음이 확인됨 |
| 조치 1 | tradeOffsAndExceptions 완화 원복 |
| 조치 2 | specificity는 짐작 당시 근거가 약했으나, 실측+원문 대조 이중 검증 결과 실제로 타당함이 확인되어 유지 |
| 조치 3 | technicalAccuracy는 처음부터 실측 기반으로 원문 대조 후 정밀 조정 (turn4 패턴만 완화, turn5는 답변 자체 문제가 섞여 있어 제외) |
| 교훈 | "짐작으로 판단하지 않고 실측으로 검증한다"는 SSOT 원칙이 실제로 작동한 사례. 결과가 우연히 맞아도, 검증 없는 판단은 위험할 수 있음을 재확인 |

### 2-40. #164 2차 조정(tradeOffsAndExceptions 원복 + technicalAccuracy 정밀 완화) 후 Lv.2 최종 재검증 — 여전히 80점 미달

동일 이력서(커머스, resumeId 시딩)·동일 답변 세트(#2-38 세션 41과 동일 텍스트)로 새 세션(42)을
Lv.2(interviewerId=2)로 재진행. 최초 4문항 17/15/13/11=56점(weakestQuestionId=4) → 꼬리질문
12점 → **최종 68점, 80점 미달**.

| 항목 | 내용 |
|---|---|
| 결과 | 68점. #2-38(70점, 조정 전)보다 오히려 2점 낮음 |
| 교란 요인 | 이번 세션의 turn4 질문이 "Race condition 시나리오 2개 이상 제시"를 명시적으로 요구하는
  더 구체적인 문구로 생성됨(#2-38 세션의 turn4는 일반적인 "동기화 전략 설계" 질문). 재사용한
  답변 텍스트가 이 특정 요구를 충족하지 못해 turn4/turn5 점수(11점/12점)가 낮게 나온 것으로
  보이며, 이는 technicalAccuracy 완화 효과와 무관하게 세션마다 질문 난이도가 달라지는
  기존에 인지된 리스크(§4 "질문 난이도 편차")가 그대로 재현된 것 — 순수 rubric 효과만
  분리해서 판단하기 어려움 |
| #167 회귀 확인 | 이번에도 overallFeedback이 fallback으로 대체됐지만 evaluations/totalScore/passed는
  정상 반영됨을 API 응답과 `judgment_results` DB 직접 조회로 재확인(score=68, passed=false) —
  #167/#168 로직이 #164 2차 조정 후에도 여전히 정상 동작 |
| 판단 | n=1(2차 조정 후 기준)로는 결론을 내리기 이름. 질문 난이도 편차라는 별도 변수가 섞여
  있어 이번 라운드만으로 "2차 조정도 효과 없다"고 단정할 수 없음. 표본을 더 늘리거나,
  질문 생성 결과를 세션 간 고정하는 방법(예: 동일 questions 세트 재사용)이 있어야 rubric
  변경 효과를 깨끗하게 분리해 측정 가능 |
| 사후 조치 | 임시 유저(9010)·이력서·세션(42) cascade 삭제 확인(재조회 NOT_FOUND). 시딩/조회/정리용
  임시 자바 스크립트 6개 전부 삭제. `application-local.yml` mock 복귀, real 블록 주석 처리,
  API 키 유지. bootRun/gradle daemon 종료 |

### 2-41. PM 지시 — technicalAccuracy 완화 효과 통제 비교(질문/답변 완전 고정, 구간 기준만 변경)

#2-40의 세션 재검증은 매번 새로 생성되는 질문 난이도 편차가 섞여 rubric 순수 효과를 분리하지
못한다는 한계가 있었음(#2-40 참고). 이를 보완하기 위해 PM 지시로 **질문·답변을 완전히
고정**하고 시스템 프롬프트의 technicalAccuracy 구간 기준만 A(원복)/B(정밀완화)로 바꿔
직접 `ClaudeLlmClient`를 호출(세션/DB 미경유, 표준입력 standalone 스크립트)해 채점 결과를
비교했다.

- 첫 시도는 새로 `generateQuestions`를 1회 호출해 얻은 질문 세트에 기존 준비된 답변을
  turn 순서로 그대로 매핑했으나, 이번 생성에서 질문 순서·주제 배분이 달라져 답변과 질문이
  서로 어긋나는 문제가 발생(예: turn1이 "FOR UPDATE 동작 원리 설명"을 물었는데 답변은
  페이지네이션 얘기) — 결과가 무의미해 폐기하고 재설계함(이 라운드는 로그에 반영하지 않음,
  API 호출 자체는 소모됐으나 유효한 데이터가 아니므로 별도 기록 생략).
- 재설계: LLM 질문 생성을 아예 거치지 않고, **#2-38 세션 41에서 실제로 받았던 turn4
  (MySQL/Redis 동기화)·turn5(청크 롤백/멱등성) 질문+답변 원문을 그대로 하드코딩**해 완전
  고정. expectedAnswer는 당시 feedback이 요구했던 패턴(Write-Through/TTL/이벤트소싱/
  주기적 재조정/Circuit Breaker/메시지큐, 보상 트랜잭션/역순 롤백/상태 컬럼)을 반영해 직접
  작성. 이 고정 입력을 시스템 프롬프트만 바꿔 2회(A/B) 채점.

| 항목 | 내용 |
|---|---|
| turn4(MySQL/Redis) | A: technicalAccuracy=6/8, score=17 · B: technicalAccuracy=6/8, score=18 |
| turn5(청크 롤백) | A: technicalAccuracy=4/8, score=14 · B: technicalAccuracy=4/8, score=14 |
| 합계 | A(원복)=31, B(정밀완화)=32, 차이 +1 |
| 판단 | **technicalAccuracy 원시 점수 자체는 두 turn 모두 A=B로 동일** — 구간 기준 문구를
  완화해도 실제 채점 결과는 변하지 않음. 총점 차이 +1은 다른 세부항목의 샘플링 잡음(동일
  입력이라도 LLM 응답은 완전히 결정적이지 않음) 수준으로 보임. B의 feedback도 "명시한
  CDC/이벤트소싱... 등 핵심 설계 요소들이 누락" 등 A와 사실상 동일한 논조로, LLM이
  expectedAnswer에 나열된 구체 패턴과의 비교를 rubric 문구와 무관하게 계속 수행하는
  것으로 보임 — **1차 시도(#2-37/#164 원인 분석)에서 세운 "구간 문구를 완화하면 점수가
  오를 것"이라는 가설이 이 통제 비교에서는 지지되지 않음** |
| 시사점 | technicalAccuracy 점수를 실제로 올리려면 구간 설명 문구 조정만으로는 부족하고,
  expectedAnswer 자체의 구체성 수준을 낮추거나(모범답안이 너무 상세하면 그 자체가 감점
  기준이 됨), 채점 지시문에 "expectedAnswer는 만점 기준이 아니라 참고용"이라는 명시적
  안내를 추가하는 등 다른 접근이 필요할 수 있음 — 팀 판단 필요 |
| 부가 확인 | 임시 스크립트(`TechAccuracyAbCompare.java`, `git diff` 소스+클래스 모두 삭제 확인)
  외에 세션/DB/애플리케이션 실행이 전혀 필요 없는 방식이라 bootRun·시딩·정리 절차 불필요 |

### 2-42. expectedAnswer 노출/참조 범위 전수 확인 — API·화면·DB·타 기능(#146/#122) 겹침 없음

technicalAccuracy 완화가 효과 없었던 원인 후보로 expectedAnswer 자체의 처리 방식을 점검할
필요가 있어(#2-41 시사점), expectedAnswer가 생성되는 프롬프트와 이후 흐름 전체를 코드
기준으로 추적.

| 항목 | 내용 |
|---|---|
| 생성 프롬프트 | 최초 4문항: `prompts/question-generation/user.txt` — "출력 검증 규칙"에
  "expectedAnswer는 API 응답 화면에 노출되지 않고 채점 로직 내부에서만 쓰인다는 전제로
  작성하세요"라고 명시. 꼬리질문(turn5): `prompts/question-generation/follow-up-user.txt`
  경유 `FollowUpGenerationResponse.expectedAnswer()`로 별도 생성 |
| API 응답 노출 | 없음 — `GeneratedQuestion`/`FollowUpGenerationResponse`의 expectedAnswer가
  어떤 Controller 응답 DTO에도 매핑되지 않음(IS-001 응답은 questionId+question만 반환) |
| 화면(히스토리 #122) 노출 | 없음 — `InterviewHistoryService.getDetail()`은 `Message`
  (question/answer 텍스트)와 `JudgmentResult.overallFeedback`만 조회, `Question` 엔티티나
  `expectedAnswer`를 전혀 참조하지 않음 |
| 유출 방어장치 | `CareerReportValidator.PROHIBITED_TERMS`에 `"expectedAnswer"` 문자열
  자체가 금지어로 등록되어 있어, 리포트 텍스트에 이 단어가 그대로 새면 검증이 실패하도록
  방어돼 있음(다른 금지어: Transcript/모범답안/confirmedScore/루브릭) |
| DB 저장 | `questions` 테이블(`expected_answer` TEXT NOT NULL, PK=`message_id`,
  `Question` 엔티티). `QuestionRepository`는 커스텀 쿼리 없이 JpaRepository 기본 메서드뿐 |
| DB 참조처 | 유일한 호출자는 `InterviewService` — 생성 시 `save()`(질문생성 1회+꼬리질문
  1회), 채점 시 `findQuestionAnswerPair()`(785~797행)로 읽어 `QuestionAnswerPair`에
  담아 LLM 채점 프롬프트에만 주입. judgment(③) 도메인의 `QuestionScore`/
  `RawQuestionEvaluation`은 이름만 비슷한 완전히 별개 타입 — 교차 참조 없음 |
| #146/#122 겹침 | 없음 — #146이 확립한 turn 구조(1~4+꼬리5)가 지금 Question/Message
  모델 그대로 유지되고 있고, #122 히스토리는 Question 엔티티를 아예 건드리지 않아
  겹치는 코드 경로 자체가 없음 |
| 결론 | expectedAnswer는 설계 의도대로 완전히 내부 전용(채점 프롬프트 조립용)으로만
  쓰이고 있고, 유출 경로나 다른 기능과의 의도치 않은 결합은 발견되지 않음. #2-41에서
  본 "정밀완화가 효과 없음" 현상의 원인은 expectedAnswer 노출/오염 문제가 아니라,
  expectedAnswer 자체의 상세도(모범답안이 특정 패턴명을 다수 나열)와 LLM이 그 상세도를
  rubric 문구와 무관하게 비교 기준으로 계속 사용하는 채점 동작 자체에 있는 것으로 보임 |

### 2-43. PM 지시 — expectedAnswer 상세도 축소("핵심 원리 2~3개+예시 1개") A/B 통제 비교

#2-42에서 원인 후보로 지목된 expectedAnswer 상세도를 실제로 낮춰서 효과를 검증. PM 지시로
`prompts/question-generation/user.txt`/`follow-up-user.txt`의 expectedAnswer 생성 지시를
"핵심 원리 2~3개 + 대표 예시 1개" 수준으로 완화(구체 패턴을 다수 나열하게 하던 기존 지시 및
few-shot 예시 모범답안 텍스트 전부 교체). 컴파일/테스트(451개) 통과 확인 후, #2-41과 동일한
turn4/turn5 질문·답변·시스템 프롬프트(technicalAccuracy 포함 현재 상태)를 고정하고
expectedAnswer만 A(원래=상세, #2-41과 동일 텍스트)/B(축소=간결, 새 지시 스타일로 직접 작성)로
바꿔 채점(직접 LlmClient 호출, 세션/DB 미경유).

| 항목 | 내용 |
|---|---|
| turn4(MySQL/Redis) | A(상세): technicalAccuracy=6/8, score=19 · B(간결): technicalAccuracy=6/8, score=16 |
| turn5(청크 롤백) | A(상세): technicalAccuracy=4/8, score=15 · B(간결): technicalAccuracy=4/8, score=12 |
| 합계 | A(상세)=34, B(간결)=28, 차이 **-6 (B가 오히려 낮음)** |
| 판단 | technicalAccuracy 원시 점수는 이번에도 A=B로 완전히 동일 — expectedAnswer 상세도를
  낮춰도 technicalAccuracy 자체는 변하지 않음. 반면 **총점은 오히려 6점 하락**했는데,
  feedback을 보면 B(간결)가 콕 집어 명시한 단 1개 예시(재시도 큐, 반대 부호 보정 항목)를
  답변이 정확히 언급하지 않았다는 이유로 다른 세부항목에서 더 크게 감점됨. 즉 모범답안을
  "패턴 여러 개 중 일부만 맞아도 되는" 구조에서 "지정된 단 하나의 예시를 맞혀야 하는"
  구조로 바꾼 셈이 되어, 표적이 더 좁고 날카로워지는 역효과가 발생 |
| 시사점 | "모범답안 상세도를 낮추면 채점이 관대해진다"는 가설은 이 통제 비교에서 **반증됨**.
  LLM은 rubric 문구나 expectedAnswer 길이와 무관하게 "답변이 expectedAnswer에 있는
  구체적 내용을 얼마나 커버했는가"를 비교 기준으로 계속 사용하는 것으로 보이며, 오히려
  expectedAnswer를 좁고 구체적인 예시 1개로 좁힐수록 그 1개를 못 맞혔을 때의 감점
  폭이 커질 수 있음. technicalAccuracy 등 세부점수를 실질적으로 완화하려면 (a) rubric
  문구 조정(#2-41에서 이미 무효 확인), (b) expectedAnswer 상세도 조정(이번 라운드에서
  역효과 확인)과는 다른 접근 — 예: 채점 지시문에 "expectedAnswer는 하나의 예시일 뿐
  다른 정당한 접근도 인정" 같은 명시적 관용 지침을 추가하는 방향을 검토할 필요 |
| 후속 조치 | `prompts/question-generation/user.txt`/`follow-up-user.txt` 변경은 **롤백하지
  않고 그대로 유지**(PM 지시가 실험이 아니라 반영이었고, 결과가 나빴다고 해서 코드
  변경분을 되돌리라는 지시는 없었음 — 팀 판단 필요 시 별도 롤백 이슈로 처리) |
| 부가 확인 | 임시 스크립트(`ExpectedAnswerAbCompare.java`) 소스+클래스 삭제 확인. 이번에도
  세션/DB/애플리케이션 실행 불필요 |

### 2-44. PM 지시 — technicalAccuracy 7~8 구간에 "2개 이상 부합" 개수 기준 명시 후 A/B 통제 비교

#2-43이 역효과로 반증되자, expectedAnswer는 원래(상세) 상태로 원복(커밋으로 #2-43의
question-generation 프롬프트 변경 되돌림)하고, 대신 채점 프롬프트(`prompts/scoring/system.txt`)
쪽에 손을 댐. "관용을 베풀어라" 식 모호한 문구 대신, technicalAccuracy 7~8 구간에
**"expectedAnswer에 제시된 예시·구현 패턴 중 2개 이상을 개념적으로 부합하게 다루면
만점 구간으로 판단하라"**는 명시적 숫자 기준을 추가(질문이 명시적으로 요구한 핵심 개념을
아예 안 다루면 예외라는 기존 단서는 유지). 컴파일/테스트(451개) 통과 확인 후, #2-41/#2-43과
동일한 turn4/turn5 질문·답변·expectedAnswer(상세, 원복 상태)를 고정하고 시스템 프롬프트의
technicalAccuracy 7~8 구간 문구만 A(개수 기준 없음, #2-37 시절 원문)/B(2개 이상 기준 명시,
현재 커밋 상태)로 바꿔 채점.

| 항목 | 내용 |
|---|---|
| turn4(MySQL/Redis) | A: technicalAccuracy=6/8, score=19 · B: technicalAccuracy=6/8, score=19 |
| turn5(청크 롤백) | A: technicalAccuracy=5/8, score=16 · B: technicalAccuracy=4/8, score=15 |
| 합계 | A(원복)=35, B(개수기준)=34, 차이 **-1** |
| 판단 | turn4는 A=B로 동일. turn5는 오히려 **B가 1점 더 낮음**(technicalAccuracy 5→4).
  숫자로 명확히 못박은 "2개 이상 부합 시 만점"이라는 구체적 기준조차 실제 채점 결과를
  개선하지 못했고, 표본 하나(turn5)에서는 소폭 악화까지 나타남. #2-41(구간 문구 정밀완화),
  #2-43(expectedAnswer 축소)에 이어 **세 번째 연속으로 rubric/expectedAnswer 쪽 프롬프트
  개입이 technicalAccuracy를 유의미하게 끌어올리지 못함** |
| 누적 결론 | 지금까지 시도한 세 방향(구간 문구 완화 → expectedAnswer 축소 → 숫자 기준 명시)이
  전부 효과가 없거나 역효과였다는 점에서, technicalAccuracy 채점이 프롬프트 문구 조정에
  잘 반응하지 않는 것으로 보임. LLM이 실제로는 "질문에 대한 전형적으로 좋은 답변"이라는
  자체 기준(모델 내부 지식)으로 채점하고, system.txt의 세부 구간 설명은 채점 결과에
  큰 영향을 못 주는 장식적 지침에 가까울 가능성. 프롬프트 문구를 더 튜닝하는 접근보다,
  (a) 이 정도 채점 엄격도를 있는 그대로 받아들이고 80점 커트라인 자체를 ③ 영역에서
  재검토하거나, (b) 좀 더 근본적으로 다른 채점 방식(예: few-shot 채점 예시 추가, 온도
  조정 등)을 검토하는 방향이 다음 단계로 필요해 보임 — 팀 판단 필요 |
| 부가 확인 | 임시 스크립트(`CountThresholdAbCompare.java`) 소스+클래스 삭제 확인.
  세션/DB/애플리케이션 실행 불필요 |

---

## 3. 최종 적용 방식 요약

- **모델**: Claude Haiku 4.5, 현재 코드 기준 세션당 AI 호출 4회
- **질문 생성**: 이력서+키워드(1개) 동적 주입, 4문항 + 모범답안 동시 생성
- **채점**: 문항당 20점(8/4/3/3/2), 다섯 문항 합계 100점 산출
- **꼬리질문**: 4문항 중 최저점 1개에 한정, turn 5만 신규 채점하고 최초 1~4 확정 점수와 합산해 Lv.1 60점/Lv.2 80점 이상 통과
- **방어로직**: JSON 스키마 검증, 최대 1회 재요청, 점수 클램핑

---

## 4. 아직 열려있는 리스크 (인지하고 있으나 미해결)

| 리스크 | 대응 방향 |
|---|---|
| 실 LLM 연동 시 코드펜스(마크다운) 스트리핑 필요 | 2주차 실연동 시 파싱 전처리 추가 예정 |
| 세션당 4회 정상 호출 구조에서 실제 API 사용량이 예산 안에 들어오는지 재검증 필요 | 2주차 실연동 최우선 확인 사항 |
| 질문 난이도가 세션마다 AI 재량에 따라 편차 발생 가능 | 완전한 균일화는 한계로 인지, 발표 시 명시 |

---

*이 문서는 팀 슬랙 논의 및 디스코드 기록을 기반으로 작성되었습니다. 세부 프롬프트 원문 및 테스트 로그는 각 담당자(김한비) 확인 후 보강 예정입니다.*
