# llm-cost-policy.md — LLM 비용/모드 운영 정책

> 근거: `[기획서] 커리어 던전 v5.1.pdf` 12장(리스크: 무제한 재도전 비용 방어), 18장(테스트
> 전략: Mock 활용 전략, API 키 관리). 관련 요구사항: NFR-11, `CM-001`. 이 문서는
> `docs/ai/SHARED.md` §3 역방향 추적 ⑥(비용/시간 가드)의 판단 기준입니다.

## 1. 기본 원칙: Mock이 기본값

- **개발 중에는 항상 Mock LLM 모드(고정 JSON 반환)를 사용한다.** 실제 벤더 API를
  호출하는 코드 경로는 기본적으로 비활성화되어 있어야 한다.
- **실 API 호출은 다음 두 상황에만 한정한다**: (1) 통합 테스트, (2) 데모 리허설/데모
  당일. 그 외 상황(로컬 개발, 단위 테스트, 리뷰)에서는 Mock을 쓴다.
- Mock 구현체는 `LlmClient` 인터페이스의 한 구현체일 뿐이며, 어떤 구현체를 쓸지는
  설정(프로필/환경변수)으로 전환한다. 도메인 코드가 "지금 Mock인지 실제인지"를 몰라도
  동작해야 한다(NFR-09 벤더 추상화와 동일한 원칙).
- 최종 커리어 리포트 실호출은 기본 비활성 수동 테스트
  `ClaudeCareerReportRealApiTest`를 사용한다.
  `.\gradlew.bat test --tests "*ClaudeCareerReportRealApiTest" -DrunClaudeCareerReportTest=true
  -DcareerReportSampleCount=10`처럼 명시적으로 활성화한다. 표본 수는 1~20만 허용하고,
  API 키는 배포 서버와 동일한 `LLM_ANTHROPIC_API_KEY` 또는 `ANTHROPIC_API_KEY`
  환경변수로만 전달한다. 일반 `check`에서는 실행하지 않으며 실행 결과와 호출 횟수는
  즉시 `docs/ai/api-usage-log.md`에 기록한다.
- 이력서 5종의 Lv.2 적합/부적합 답변을 빠르게 비교하는 실호출은 기본 비활성 수동 테스트
  `ClaudeFiveResumeLv2RealApiTest`를 사용한다. 실행 프로세스에
  `RUN_CLAUDE_FIVE_RESUME_LV2_TEST=true`와 위 API 키 환경변수를 전달했을 때만 총 10건을
  병렬도 2로 실행한다. 질문은 고정하고 최초·최종 채점 및 리포트만 실제 운영 경로로 호출하며,
  정상 20회와 각 단계별 최대 1회 재요청을 포함해 최대 40회로 제한한다. 일반 `check`에서는
  실행하지 않으며 결과 JSON은 `build/reports/lv2-five-resume-real-api-results.json`,
  실제 호출 횟수는 `docs/ai/api-usage-log.md`에 즉시 기록한다.
- Lv.2 채점 밸런스 전후 비교는 기본 비활성 수동 테스트
  `ClaudeLv2ScoringBalanceRealApiTest`를 사용한다. `runClaudeLv2BalanceTest=true`일 때만
  실제 API를 호출하며, `lv2BalancePromptVariant=baseline|current`와
  `lv2BalanceSampleCount`로 기준선·현재 프롬프트 및 표본 수를 선택한다. 표본 수는 1~20만
  허용하고 API 키는 `LLM_ANTHROPIC_API_KEY` 또는 `ANTHROPIC_API_KEY` 환경변수로만
  전달한다. 일반 `check`에서는 실행하지 않고 실제 호출 직후 사용량을
  `docs/ai/api-usage-log.md`에 기록한다.

## 2. 예산 상한 (1차 방어선)

- 공용 API 키 **1개**를 통합 관리한다. 개인 키를 임의로 추가하지 않는다.
- **월 예산 상한(hard limit)**을 설정한다. 이것이 "무제한 재도전 시 비용 증가" 리스크에
  대한 1차 방어선이다(기획서 12장).
- 실사용 모니터링 후 필요하면 재도전 횟수 제한(스트레치골, `FEAT-21` "AI 재도전 하루
  5개 제한")을 2차 방어선으로 추가 검토한다. **MVP 범위에는 재도전 횟수 제한이 없다** —
  예산 상한만으로 1차 방어한다는 점을 착각하지 않는다.
- API 키 관리: 환경변수로만 관리한다. 하드코딩 금지, GitHub 커밋 금지
  (`.gitignore`에 `.env`류가 포함되어 있는지 주기적으로 확인).

## 3. 세부 모델 선택 (확정: Haiku 4.5)

- 벤더는 Claude로 확정(`docs/adr/ADR-003-llm-vendor-selection.md`). 세부 모델은
  **Haiku 4.5로 확정**한다 — Mock 답변 기준 Haiku/Sonnet 비교 테스트에서 채점 방향·
  feedback 품질 동등, 점수 편차는 기획서 §12 감수 범위 내, 비용이 3주 예산에 더
  적합해 채택 (`FEAT-13`, `docs/adr/ADR-007-llm-model-selection-haiku45.md`).
- 확정 전까지 모델명을 코드에 하드코딩하지 않는다 — 설정값으로 분리한다.
- 모델 선택 기준(확정 후 이 절을 갱신할 것): 채점 일관성(NFR-08), 응답 속도(폴링
  타임아웃과 연동, NFR-04), 비용.

## 4. 호출 횟수 가드

- 현재 코드 기준 정상 세션의 총 AI 호출 예산은 세션당 4회다.
  ①질문생성, ②최초채점, ③꼬리질문생성, ④최종채점으로 계산한다.
- 1번 호출(FR-03)은 최초 질문 4개 + 모범답변 4개를 함께 생성한다. 최초 질문과
  모범답변을 분리 호출하면 질문 생성 영역의 호출 수가 불필요하게 늘어나므로 금지한다(ADR-013).
- 2번 호출(FR-04)은 최초 4문항 채점이다.
- 3번 호출은 최저점 문항의 원래 질문, 사용자 답변, 채점 feedback을 바탕으로 꼬리질문 1개와 모범답안을 생성한다.
- 4번 호출(FR-04, 이슈 #60, #147)은 turn 5 한 건만 최종 채점한다. 최초 1~4는 다시 채점하지
  않고 서버 확정 점수를 재사용하며, 종합 피드백용 읽기 전용 컨텍스트로만 전달한다.
- JSON 스키마 검증 실패 시 **최대 1회 재요청**까지만 허용한다(NFR-05). 재요청 횟수를
  이 상한보다 늘리는 변경은 반드시 이 문서를 먼저 갱신한다 — 재요청은 곧 추가 비용이다.
  현재 최악 케이스 호출 수는 8회다.

## 5. 역방향 추적 ⑥ 체크리스트에 대응하는 질문

새 코드가 LLM을 호출한다면 아래를 확인합니다.

- [ ] 기본 프로필에서 Mock 구현체가 선택되는가?
- [ ] 이 호출이 세션당 호출 횟수 기준(현재 총 AI 호출 4회)을 넘지 않는가?
- [ ] JSON 파싱 실패 시 재요청이 최대 1회로 제한되어 있는가?
- [ ] 모델명이 하드코딩되지 않고 설정값으로 분리되어 있는가?
