# 채점 프롬프트 설계 v3 (20점 배점 반영)

## 1. 자료 설명

채점 프롬프트 설계 과정을 기록합니다.

> **개정 이력 (v2→v3, 2026-07-22)**: `채점_기준에_대한_로직_설계_v2.md` 확정에 따라 배점 25점→20점(8/4/3/3/2), 통과 기준 단일 80점→레벨별(Lv1 60/Lv2 80) 반영. 예상답변 단일호출 방식, JSON 리스크 대응 원칙(§4)은 변경 없음 — 그대로 유지.

---

## 2. 자료 첨부

기준 문서: `채점_기준에_대한_로직_설계_v2.md` (팀 공식본) + 기획서 v5.5 §4, §10
변경 사항: 문항당 배점 25점(10/5/4/3/3) → **20점(8/4/3/3/2)**, 문항 수 3+1 → **4+1(총5)**
"예상 답변 생성 후 비교" 방식은 v2와 동일하게 단일 호출 내 내부 추론으로 유지.

---

## 예상 답변 생성 방식 (변경 없음)

**방법 B — 단일 호출 방식. 한 번의 호출 안에서 ✅**

- 시스템 프롬프트에 "먼저 속으로 모범 답안을 생각하고(출력하지 말고), 그 다음 채점 결과만 JSON으로 줘"라고 지시 → LLM 호출 **1번**으로 끝
- "정상 케이스는 무조건 1번 호출", "재요청"은 파싱 실패했을 때만 쓰는 여분으로 유지
- 7/13 Haiku/Sonnet 실측 검증 완료 — 단일 호출로 채점 결과 정상 출력 확인됨 (배점 변경과 무관하게 이 검증 결과는 그대로 유효)

---

## 1. 세부 채점 기준 (v3 — 20점 반영)

**기술적 정확성 (0~8점)**

| 점수 | 기준 |
| --- | --- |
| 7~8 | 핵심 개념이 정확하고 세부 설명에도 오류 없음 |
| 5~6 | 핵심은 정확하지만 일부 표현·세부 내용 부족 |
| 3~4 | 기본 개념은 이해하나 중요한 누락이나 작은 오류 있음 |
| 1~2 | 일부 맞지만 핵심 개념을 혼동함 / 관련 키워드만 언급 |
| 0 | 무응답, 질문과 무관 또는 완전히 잘못된 답변 |

**핵심 내용 충족도 (0~4점)**

| 점수 | 기준 |
| --- | --- |
| 4 | 필수 포인트를 모두 설명 |
| 3 | 대부분 설명, 일부 세부 내용 누락 |
| 2 | 절반 정도 설명 |
| 1 | 키워드만 언급 |
| 0 | 관련 내용 없음 |

**근거·판단 과정 (0~3점)**

| 점수 | 기준 |
| --- | --- |
| 3 | 선택 이유와 판단 과정이 논리적으로 연결됨 |
| 2 | 근거는 있으나 일부 과정 생략 |
| 1 | 이유는 있지만 일반적이거나 모호함 / 결론만 있고 근거 없음 |
| 0 | 판단 과정이 없거나 모순됨 |

**구체성·실무 연계 (0~3점, 변경 없음)**

| 점수 | 기준 |
| --- | --- |
| 3 | 실제 상황, 구현 방법, 수치, 결과를 구체적으로 설명 |
| 2 | 프로젝트 사례는 있으나 상세 설명 부족 |
| 1 | 추상적이거나 일반적인 예시만 설명 |
| 0 | 실무 사례나 구체적 설명 없음 |

**트레이드오프·예외 (0~2점)**

| 점수 | 기준 |
| --- | --- |
| 2 | 장단점, 대안, 예외·장애 상황까지 설명 |
| 1 | 단점이나 대안 중 일부 설명 |
| 0 | 트레이드오프를 전혀 고려하지 않음 |

---

## 2. 프롬프트 템플릿 (v3)

### 시스템 프롬프트

```
너는 백엔드 개발 채용 면접의 채점관이다.
너의 역할은 지원자의 답변을 정해진 루브릭에 따라 채점하는 것이며,
지원자를 격려하거나 저지하는 것이 아니라 정확하고 일관된 평가를 내리는 것이다.

[평가 대상 정보]
- 지원자 이력서/포트폴리오 발췌: {resume_excerpt}
- 선택된 주제 키워드: {keyword}
- 면접 질문: {question}
- 지원자 답변: {answer}

[채점 절차 — 아래 두 단계를 반드시 순서대로, 내부적으로 수행하라. 1단계 결과는 출력하지 마라.]

1단계 (내부, 출력 금지):
이 질문에 대해 백엔드 개발자가 제시할 수 있는 모범 답안의 핵심 포인트를
스스로 먼저 정리하라. 특정 언어나 프레임워크의 구현 디테일이 아니라,
개념적으로 반드시 언급되어야 할 요소(원리, 근거, 트레이드오프)를 기준으로 삼는다.

2단계 (내부, 출력 금지):
1단계에서 정리한 모범 답안 기준과 지원자의 실제 답변을 비교하여,
아래 5개 항목 각각을 독립적으로 채점하라. 항목 간 점수를 서로 보정하지 마라.

[채점 원칙]
- 특정 언어·프레임워크 문법이 아니라 백엔드 개념의 정확성을 평가하라.
- 이력서에 없는 경험을 답변이 언급해도 사실 여부를 판정하지 말고,
  설명의 논리성과 구체성만 평가하라 (사실 검증은 채점 범위 밖).
- 아래 세부 기준표의 점수 구간을 그대로 따르라. 자체적으로 기준을 완화하거나
  강화하지 마라.

[채점 루브릭]

1. 기술적 정확성 (0~8점)
   7~8: 핵심 개념이 정확하고 세부 설명에도 오류 없음
   5~6: 핵심은 정확하지만 일부 표현·세부 내용 부족
   3~4: 기본 개념은 이해하나 중요한 누락이나 작은 오류 있음
   1~2: 일부 맞지만 핵심 개념을 혼동함 / 관련 키워드만 언급
   0: 무응답, 질문과 무관 또는 완전히 잘못된 답변

2. 핵심 내용 충족도 (0~4점)
   4: 필수 포인트를 모두 설명
   3: 대부분 설명, 일부 세부 내용 누락
   2: 절반 정도 설명
   1: 키워드만 언급
   0: 관련 내용 없음

3. 근거·판단 과정 (0~3점)
   3: 선택 이유와 판단 과정이 논리적으로 연결됨
   2: 근거는 있으나 일부 과정 생략
   1: 이유는 있지만 일반적이거나 모호함 / 결론만 있고 근거 없음
   0: 판단 과정이 없거나 모순됨

4. 구체성·실무 연계 (0~3점)
   3: 실제 상황, 구현 방법, 수치, 결과를 구체적으로 설명
   2: 프로젝트 사례는 있으나 상세 설명 부족
   1: 추상적이거나 일반적인 예시만 설명
   0: 실무 사례나 구체적 설명 없음

5. 트레이드오프·예외 (0~2점)
   2: 장단점, 대안, 예외·장애 상황까지 설명
   1: 단점이나 대안 중 일부 설명
   0: 트레이드오프를 전혀 고려하지 않음

[출력 형식]
1단계, 2단계의 사고 과정을 출력하지 말고, 아래 JSON만 출력하라.
다른 텍스트, 설명, 마크다운 코드블록 표시(```)를 절대 포함하지 마라.

{
  "기술적정확성": <0-8 정수>,
  "핵심내용충족도": <0-4 정수>,
  "근거판단과정": <0-3 정수>,
  "구체성실무연계": <0-3 정수>,
  "트레이드오프예외": <0-2 정수>,
  "feedback": "<부족한 지점을 짚는 2~3문장, 존댓말>"
}
```

### 호출 파라미터 (변경 없음)

- `temperature`: 0.2~0.3 (ADR §16 — 채점 일관성용, 질문 생성과 분리)
- `max_tokens`: feedback 문장이 잘리면 JSON 파싱 자체가 깨지므로 여유 있게 설정

---

## 3. Few-shot 예시 (v3 — 새 배점 범위 내 값 확인)

기존 예시 값(정확성4/충족도2/근거1/구체성0/트레이드오프0)은 **새 배점 상한(8/4/3/3/2) 안에 그대로 들어와서 값 자체는 수정 불필요**. 다만 항목명 순서·범위 표기만 v3 기준으로 맞춤.

```
예시 입력 답변: "캐시는 DB 부하를 줄여서 씁니다."
예시 출력:
{
  "기술적정확성": 4,
  "핵심내용충족도": 2,
  "근거판단과정": 1,
  "구체성실무연계": 0,
  "트레이드오프예외": 0,
  "feedback": "캐시 사용 목적은 언급했으나 캐시 무효화 전략이나 정합성 문제 등
  구체적인 원리 설명이 없습니다. 실제 적용 경험이나 트레이드오프 언급이
  추가되면 좋겠습니다."
}
```

> ⚠️ 실제 운영 전, 새 배점(8/4/3/3/2) 기준으로 few-shot 예시를 1~2개 더 실API로 검증 권장 — 기존 3주차 초 few-shot 재검증(#89)과 같은 방식으로, 최소 정상케이스 1건은 실측 확인.

---

## 4. JSON 리스크 관련 (v3 — 문항 수 변경 반영)

- **초기 질문 4개 × 5항목 = 세션당 최초채점 호출 1회에 최대 20개 개별 숫자**를 정확히 산정해야 함 (기존 3문항×5항목=15개 대비 증가) → 파싱 실패·항목 누락 리스크가 v2보다 커짐.
- 이미 계획된 "최대 2회 재요청 + 스키마 검증 + 점수 클램핑"(§7 도전과제 #5) 방어가 더 중요해짐 — 최용성 님과 스키마 검증 시점에 **5개 항목 키가 모두 존재하는지, 각 항목 값이 새 배점 상한(8/4/3/3/2)을 벗어나지 않는지**까지 검증 범위에 포함되어야 함. (기존엔 10/5/4/3/3 기준이었으므로 클램핑 로직의 상한값도 같이 갱신 필요)
- 클램핑만으로 부족한 경우(키 자체가 누락된 경우)는 값 보정이 불가능하므로 재요청 트리거 조건에 "필수 키 누락"을 명시적으로 포함해야 함.

---

## 5. 백엔드 연결 (③ 평가·게이지·해금 도메인) — v3 개정

LLM이 반환한 5개 항목 점수를 ③ 도메인에서 합산 → `score`(0~20) 산출 →
**5문항(초기4+꼬리질문1) 합산 시 0~100** →
**레벨별 커트라인 통과 판정: Lv.1(널널한 대리) 60점 / Lv.2(깐깐한 과장) 80점**
(기존 단일 80점 통과 기준은 폐기, 레벨별 분기로 대체)

**API 응답·화면에는 5개 항목 노출 없이 `score` + `feedback`만 전달** (기획서 §10 확정,
재논의 대상 아님 — 이 원칙은 배점 변경과 무관하게 유지).

```json
{"questionId": 1, "score": 15, "feedback": "정확성은 좋았으나 구체적 사례 제시가 부족했습니다."}
```

> ⚠️ Lv.2(80점) 통과 난이도 관련 밸런스패치 별도 진행 중(담당: 김한비) — 실측 검증 후 배점/프롬프트 관대화 여부 추가 조정 가능성 있음. 확정 시 이 문서도 재개정 필요.
